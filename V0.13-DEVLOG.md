# v0.13 Dev Log

**Status:** not started. Live site is on **Beta v0.12**.

1. **Discord source material** — when v0.13 ships, distill this into a `data/updates.json` entry plus a Discord copy-paste version. **Ask what the update is called before writing either.** The changelog is written for players using the wiki, not for the owner and not as a summary of this file — see `.claude/skills/ship-a-release/SKILL.md` for the rules, which were rewritten after the v0.12 draft failed them.
2. **Engineering record** — so a future session can see what changed and why without re-deriving it from the diff.

Player-facing line first, technical detail under it. Archive to `devlogs/` once v0.13 ships.

---

## Scope

Per `PLAN-official-transition.md`, which supersedes the original v1.0 roadmap from v0.12 onward. **Confirm scope is still current before starting** — it has moved twice already.

1. **Media moderation and upload limits.** The most urgent item, and more urgent than when it was written: the gallery editor shipped in v0.12, so contributors can already upload media with no size cap and no review of the file itself. User-submitted media at Discord scale without limits is the most obvious way to get hurt.
2. **The Emotes page and its content.** The gallery type, its search and media submission all shipped in v0.12; what remains is the page.
3. **The other ten `others/` pages** scaffolded. Content is the team's.
4. **The Main Dashboard layout rework** beyond the two columns that shipped early.
5. **Site-wide progress view** in owner tools — **moved here from v0.12** (owner, 2026-08-11). "Pages That Need Work" covers characters only; with 30 people working through ~35 pages there is no single view of done / in progress / untouched. More useful now that the `others/` pages exist to appear in it.
6. **Skill Builder ID Reader** linked in. The tool-page editor it needed shipped in v0.12, so this is config plus a link.
7. **Certified Tier List** — the free-submit community ranking, registering against the tool host from v0.12. The anti-brigading rules from v0.14 should be designed with it, not after.

## Carried in from v0.12

- **The suite is ~573 specs and ~4.5 minutes, and the obvious explanations are wrong.** Measured 2026-08-11, because the first two guesses were both incorrect and each one nearly became a fix:

  | Hypothesis | Test | Result |
  |---|---|---|
  | Real Supabase calls dominate | blocked `supabase.co` entirely on a 32-navigation spec | 17.3s to 16.8s. **3%.** Only 21 of 81 spec files mock it, and it barely matters. |
  | It is CPU-bound, so more workers help | 1 / 4 / 8 workers on the same spec | 21.8s / 15.1s / 18.6s, and 8 workers *failed 2 tests*. 4x the workers buys 1.44x. |
  | `python -m http.server` is the constraint | replaced it with a Node server | **9.3 min and 31 failures** - worse. Every failure was `page.goto` timing out, because that server called `fs.statSync` per request and blocked its own event loop, where python's threaded handler does not. Reverted. |

  So: the poor worker scaling is real and still unexplained, Supabase is not the cause, and a naive Node server is worse than what is already there. **Next things to try, in order:** (1) the same Node server with async `fs.promises.stat` - the blocking I/O is a known flaw with a known fix, and it would also let 404.html be served the way GitHub Pages does, which `js/page_rescue.js` currently cannot be tested against; (2) failing that, shard CI across a matrix (`--shard=1/3`), which spreads over separate runners and helps whatever the local constraint turns out to be. Neither is urgent.
- **`js/site_utils.js` is cache-busted by content hash.** Editing it by hand means running `npm run generate` before pushing, or `npm run validate` fails. That check exists because a stale cached copy of that one file took the whole editor down live on 2026-08-10.
- **One flaky spec was fixed on the way out**, and the lesson generalises: `page-skeleton-parity` collected `[id^="tab-"]` from anywhere on the page and picked up the nested move-variant tabs `js/framedata.js` builds, so it passed or failed depending on network timing. It blocked a merge on a PR that changed only JSON and markdown.
