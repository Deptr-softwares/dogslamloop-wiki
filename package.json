{
  "scripts": {
    "test": "playwright test",
    "validate": "node scripts/validate-navigation.js && node scripts/generate-pages.js --check",
    "generate": "node scripts/generate-pages.js --write",
    "refresh-previews": "node scripts/fetch-previews.js --write",
    "refresh-registry": "node scripts/fetch-registry.js --write",
    "refresh-content": "node scripts/fetch-content.js --write"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "playwright": "^1.62.1",
    "supabase": "^2.109.1"
  }
}
