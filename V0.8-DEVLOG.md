# v0.8 Dev Log

**Status:** not started. Live site is on **Beta v0.6**. This file tracks everything happening on the v0.8 branch of work in real time, for two purposes:

1. **Discord source material** — when v0.8 ships, distill this into a `data/updates.json` entry the same way "Beta v0.6" was (see that file for the target format/tone: short, plain-language, player-facing).
2. **Engineering record** — so any future session (me or you) can see exactly what changed and why without re-deriving it from the diff.

Each entry below should have a short player-facing line first, then technical detail under it. Archive this file (`devlogs/V0.8-DEVLOG.md`) once v0.8 actually ships and start a new one for v0.9.

**Scope, per the roadmap as of 2026-08-03 (confirm this is still current before starting — see `feedback_confirm_scope_before_each_roadmap_phase`):**
1. CI automation — the Playwright suite is committed but never runs automatically (`playwright.config.js` already has `reuseExistingServer: !process.env.CI` waiting for it)
2. Fix the split-brained contributor notification system (`js/admin.js`/`site_utils.js`'s real `user_notifications` table + modal that's never unhidden, vs. `pagebuilder.js`'s sidebar badge reading a `system_inbox` table that doesn't exist in any migration)
3. `404.html`
4. Footer + basic legal (privacy policy, link the existing unlinked `LICENSE`)

---
