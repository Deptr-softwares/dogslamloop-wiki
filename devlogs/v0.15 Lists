New feature List:
- Pasting a Supabase storage media link in a table slot would render a video/image there (Strictly Supabase only, wiki-media), the media would be sized down appropiately.
- Add a new UI to playing videos (video and youtube blocks) that has control on; Stylized, Sharp, and use --color-blue (wiki default color) so it can dynamically change between character pages.
- Implement the Gallery tab for each character (different from the gallery type page, Emotes): By clicking on 'edit this tab' button in the character page. You navigate to that character Gallery tab in the editor, similar to all tabs currently. The UI for uploading is simple, no need for block edit footer similar to editting Character Profile or Playstyle section in the Overview tab. The card for the media in the gallery should have a title and a description under the media itself. The field for the actual media should get a Media link, the user need to upload that media to the Media Library first and get the link.
- Add a placeholder for images and videos (DogslamloopIcon.webp in the medias/images/ directory with Finger-Paint text above saying "Placeholder"): Currently no link video blocks would somehow get its Height compressed, and so is no link image blocks. They are weirdly rendered in as this empty box that says "I am broken"
- Add a way to find and replace (single and mass-edit) in the editor: If someone want to replace all instance of "Ryu" into "True Cannon", they would have to shift through dozens of blocks and walls of text to find them and do it one-by-one
- Add "Block Folders" to the editor: The editor main element "blocks" is the representation of Data to edit. Right now, if a page has a lots of Data, aka many blocks, an editor would have a pain scrolling through them to get to where they need to edit. A way to fix this is to add a way to group selected blocks into folders for easier navigation within the editor. This is editor facing only, normal visitor to the site won't be feeling this.
- Add better icons to characters (only apply to the roster selection in Main and Character Dashboards, you can check that I have prepared for this by getting those icons in the medias/images/ directory). When this item arrive, aka being developed, ask the owner to give you the idea on how this should look, I already have the concept image and idea in mind.
- Instead of adding a move/section/matchup/counterplay to the end of a tab everytime in the editor. Make it so that those add buttons are below the section, and you can add section in-between sections that you are on. Alongside this add a way to move the section left and right in the editor (For example, in JJS new game update, Skill 1 of this character receive a brand new variant: So I want to add this variant next to the Skill 1 card for the character, but I can't so easily cause adding move only add it at the end > The only way to do it is editting the existing skills one-by-one until the end, which is not ideal)
- Add a way to box-in a list. Visual-only change, a list would be surrounded by a box with sleek and clean border.
- Add a way to hyperlink to selected sections/tabs in a character page: For example, I mention this named tech "The Triple Drill Tech" in a Skill Overview in the Skill Tab, I would want to hyperlink it to the "Tech" section in the Overview Tab of the same character. People would click on it and get directed to it (We sorta get it with the ToC logic)
- Add an extensive user profile system. Right now you can login, and change one thing: Display Name. Nothing else. So I suggest: Changing Password (If they don't use a provider to register/login), Customizable Portfolio aka Description, Customizable User Flair (Appear alongside their Display Name when discussion on the site), A display of their current position within the site (Normal user, Trusted Editor, Reviewer, Moderator, ...), A way to make their profile private. Clicking on an username in the discussion would bring up a modal that show their profile.
- Add a new small field to the tier-editor.html: Game Version - simply let the tier list editors and the visitors to keep track of the game version their tier list was on.
- Add a way to see uploader for each media in the Media Library. (Maybe create a table called media_uploader that has Display Name, and media link, upload date as well).
- Add the newly planned v0.15 items in the memory.

Fine-tuning List:
- For the Tier List pages (Certified Tier List, Free Submit Tier List), write a logic to fetch the character current Portrait image link in the 'profile' > 'image' in page_data so it stays up-to-date, but won't need to load in everytime a tier list page is visited (saving cache egress if possible)
- Also for the Tier List pages, when hovering over character portraits, their name would pop up smoothly from underneath. The boxes that contain the character can be larger with slightly less spacing around them.
- For the Certifies Tier List editor (tier-editor.html), on the right preview panel, also load in the Introduction, Changelogs, and Reasoning, not only the Tier List. Then, add a scrollbar to the workspace on the left (right now, editting Introduction is almost impossible cause you can't scroll down). Then, instead of limiting editting tier name to one character per type, remove that limit and let the trusted tier list folks to name a tier however they want (If a tier name is long and the font size need to be shrink down, give the same treatment to the rest of the tiers' names, keeping consistency). Finally, add the custom color selecter built in v0.14 to the tier custom color selectors.
- Add a way to Open/Close Discussion thread under each character page. By default it is closed, only load in when open. Doesn't remember as well, to avoid scenario where a page is slowed down when there is too much discussion.
- Make Diff View logic in the admin.html truly able to render in all possible edit (Metadata, Stats, system type page, tools type page, gallery type page). Make the wordings also make sense for a low-technical team (DETECTING CHANGE: OVERVIEW > GENERAL STRATEGY is not exactly helpful and can be confusing to grasp what it means)
- Some system pages do not have Discord Embed. It seems that only the Main/Side/Character Dashboards and the character pages have it.
- Remove auto-fill in the Author field everytime a block is created in the Editor. Author/Contrubitors creditting is now up to that person on whenever they like to have their name there or not. (Plus it duplicates the author in the compiled list below)

Bug fixes List:
- Editting blocks (reportedly list, and very long paragraph) in the editor for some Browsers (reportedly Firefox and Microsoft Edge) would sometimes scroll the user all the way to the top of the workspace/section, which is very annoying.
- Intercepting your own edit in submission.html seemingly do not work. I suspect there is an error in the "Intercept and Edit" logic in the admin page scripts.
- Mutli-tab submission bug, when a character has 3 states. An user made edit in all 3, but changes in only 2 states gone through, one is quitely dropped.
- Merged Tickets would always break Diff View, causing Diff View to detect less changes visually
- When switching views, it would always show you the Overview page first, despite the fact that you are on other tabs, which cause confusion.
- Clicking edit this tab when you are on an ultimate tab for a character would reroute you to the overview of the base instead. Which can be confusing.