# v0.14 Dev Log

**Status:** in progress. First item landed — the reviewer-blindness bug, taken
out of order and ahead of every feature (see §9).

Live site is on **Beta v0.13**. v0.14 is the **Community** version: the first one that invites the 1.4M-member Discord to *write* rather than read.

---

## Settled with the owner, 2026-08-13

**The two tier lists are separate things.** The plan conflated them; they are not the same feature and only one of them has a brigading problem.

| | Certified Tier List | Free Submit Tier List |
|---|---|---|
| What | the existing page, unfinished | a new tool, page already scaffolded |
| Who writes | `trusted_editor` and above | anyone signed in |
| Result | a curated ranking | the **average of all votes** |
| Brigading | not applicable, editing is gated | **the whole problem** |

**Dropped:** the Main Dashboard layout rework. The owner does not recall asking for it. Cut rather than deferred, like the progress view before it.

**Threads, not forums.** Per-character discussion threads only. Full forums are post-v1.0 at the earliest, on the owner's reasoning: shipping one nobody uses is wasted effort, and that risk does not shrink by building it sooner.

**Flagged media must become unreachable.** Hiding it client-side is not enough — the owner's call, for security. That changes moderation from a rendering rule into an object lifecycle.

---

## The checklist

### 1. Per-character discussion threads

- [x] Schema: threads keyed by `page_id`, one thread per character page, with posts under it
- [x] RLS: anyone signed in posts; `viewer` (the soft ban) cannot; staff can hide or remove *(`can_moderate` lands with §2)*
- [x] Thread renders on the character page, below the tabs
- [x] Rate limiting on posting, reusing the existing `check_revision_rate_limit` shape rather than a second mechanism
- [x] Escape at every interpolation — post bodies and author names are attacker-reachable
- [x] Empty state that reads as an invitation, not a broken section

#### Shipped 2026-08-13

**One table, not two.** A `threads` table would hold nothing but a `page_id`
every post already carries — a join to learn something the child row states.
The thread is the page.

**Owner decisions, taken before the schema rather than after:** one level of
replies (a reply aimed at a reply is *flattened* to the same list rather than
rejected — the reader's intent is fine, only the depth is not); separate rate
limits; delete-only.

**Delete-only is why there is no author `UPDATE` policy at all.** RLS says
which *rows* may change, never which *columns* — so any policy letting an
author touch their own row would also let them rewrite it after three people
had replied. Removal goes through `remove_my_discussion_post`, a
`SECURITY DEFINER` RPC that can only blank the body and set the status.

**The body is blanked, not hidden.** Same reason: a removed post has to stay
readable so replies under it aren't orphans, and row-level security cannot hide
one column from a reader allowed to see the row.

Four things found while building, none of them in the plan:

- **`user_notifications` has a staff-only INSERT policy.** Correct for its
  original purpose, and it means one contributor cannot write a notification to
  another — which is exactly what a reply is. Reply notifications had to become
  a `SECURITY DEFINER` trigger.
- **`anonymize_user_by_email` hard-deletes the `auth.users` row.** A plain
  foreign key here would have made deleting anyone who had ever posted fail —
  the identical trap `pending_revisions.author_id` already documents. Fixed by
  `ON DELETE SET NULL` plus an explicit re-attribution step, so posts survive
  their author the way revisions do.
- **`author_name` would have shown the wrong name.** `getDisplayName` prefers a
  custom profile name, then the OAuth full name, then the Discord `global_name`
  claim, and only then the email prefix. Deriving it from the address in SQL
  would have printed a different name in threads than the same person shows
  everywhere else — which reads as two accounts. The trigger now mirrors that
  priority order exactly.
- **Authorship is set server-side and the client never sends it.** A revision
  can take `author_name` from the submitting page because a human reviews it
  before anyone sees it. A post is public immediately, so a client-supplied
  name would be one-line impersonation of any staff member.

`tests/discussions.spec.js`, 14 tests. Deliberately scoped to what a browser
can prove — rendering, escaping, who is offered a composer, and that the client
claims no authorship. RLS, the rate-limit trigger, the flattening and the
notification trigger are unreachable from Playwright and are probed live after
merge.

### 2. Moderator capability and moderation UI

The mechanism already exists. v0.13 shipped capability columns with `bypass_cooldown` enforced, so this follows an established, proven pattern rather than inventing one.

- [x] Add `can_moderate` to `user_roles`, defaulting false
- [x] Whitelist it in `set_user_capability` — the function rejects unknown capability names on purpose
- [x] Surface it as a second checkbox in the owner tools roster
- [x] **Moderators get into the Overseer, scoped to reports** *(owner, 2026-08-13)*
- [x] Moderation UI for threads: hide a post, remove a post, see what was removed
- [x] Moderation actions are logged, not silent — a removal nobody can audit is indistinguishable from data loss

#### Shipped 2026-08-13

**Threads shipped with staff holding a direct `UPDATE` grant, and that is now
dropped.** A reviewer with direct UPDATE can rewrite a contributor's words —
exactly the power the delete-only decision withheld from the *author*.
Withholding it from the author and handing it to staff is the wrong way round.
Every moderation action now goes through one RPC that can only set a status,
blank a body and write a log row. **There is no path in the schema by which
anybody edits somebody else's post text.**

**Two verbs, because they are genuinely different operations:**

| | Hide | Remove |
|---|---|---|
| Row | filtered out of the public result set | stays, as a placeholder |
| Body | kept | blanked |
| Reversible | yes | yes, from the log snapshot |
| For | might be a problem | definitely is |

**Hiding works as a row-level rule where item 1 could not use one.** Removing a
*row* from a result set is what RLS does; hiding a *column* from a reader
allowed to see the row is not expressible — which is why remove blanks the body
rather than trusting a policy. The two verbs fall out of that constraint rather
than being a UX preference.

**A reason is required for hide and remove, enforced in the RPC.** The log
exists so a removal can be audited, and an entry saying only that somebody
removed something is the same silence with a timestamp on it. Restore is
exempt — putting something back needs no justification.

**Restore pulls the body back out of the log**, which is the only copy after a
remove. Without it, remove would be irreversible in practice even though the
status says otherwise. Staff deliberately *cannot* restore a `removed_by_author`
post: putting someone's words back after they withdrew them is not moderation.

Also in this PR, at the owner's request: **a jump-to-discussion button on the
state-chip row.** It is a *sibling* of `#character-mode-bar`, not a child —
that bar hides itself for a character with fewer than two states, which is 20
of the 22, so a button inside it would have existed on two pages. It carries
the post count, and shows nothing rather than `0` when the thread is empty,
because an explicit zero reads as a dead feature.

Two bugs found by the tests rather than by reasoning:

- `insertBefore(badge, body)` where `body` was not yet a child of `wrap` —
  throws, taking the whole thread render down with it.
- The required-reason field's **native** validation swallowed the submit event
  before the handler ran, so the message appeared in a browser bubble while
  every other message in the file appears in the status line. `form.noValidate`
  plus a hand-rolled check, so there is one error channel.

`tests/discussions.spec.js` is now 23 tests. `tests/user-capabilities.spec.js`
grew to 9: its `.personnel-capability-box` selector had become ambiguous with
two checkboxes per row — the exact ambiguous-selector failure this project has
been caught by before — so it now addresses boxes by `data-capability`.

### 3. Certified Tier List: finish the existing page

Five items, in the owner's words.

- [x] **Introduction text editable from owner tools.** Currently not editable anywhere

#### Shipped 2026-08-14 — owner tools, and a second introduction

Three owner requests, plus one clarification that turned out to be the good
part.

**The page introduction is now data**, in a `tier_page_settings` singleton
(same `id = true` trick `site_settings` uses), edited from a new **Tier Lists**
group in owner tools. Seeded with the copy already hardcoded in the page, so
nothing disappeared and there is something to edit rather than an empty box.
The markup left in the page is the fallback, and it matches the seed — so the
page says the right thing before the fetch lands and keeps saying it if the
fetch never does.

**Assigning a list grants `trusted_editor`**, because those are one decision:
the owner picked somebody to write on the wiki under their own name, and
leaving the role to a second manual step is how a new author ends up assigned
and unable to do anything.

Two rules inside that, both worth stating:

- **Granted, never downgraded.** An admin or reviewer who gets a list keeps
  what they had. `trusted_editor` is below both, and quietly demoting somebody
  as a side effect of a favour would be a genuinely nasty bug to track down.
- **A `viewer` is refused.** `viewer` is the soft ban, so handing a banned
  account a public platform is a contradiction — far more likely a mistyped
  email than a deliberate un-banning, and un-banning should be something
  somebody does on purpose in the roster.

**Each list gained its own introduction**, above the tiers, owned by the
assignee alone and never passing through the reviewer queue — a signed tier
list is opinion, not wiki content somebody approves. Distinct from `reasoning`
on purpose: the introduction says who you are and how you rank, the reasoning
argues the specific placements from below the changelog. Collapsing them would
mean the reader meets the argument before the ranking it is about.

**And the page introduction hides while a list is open** (owner's follow-up).
That swap is the feature rather than a tidy-up: two introductions stacked would
make every list read as a subsection of the owner's page, which is the opposite
of what attributing them was for.

**One block editor, two documents.** `initStrategyBlockBuilder` keeps a single
module-level buffer, so two builders cannot coexist — switching flushes the
open one and loads the other, exactly what `editor-tabs.js` does across a tab
boundary. Saving flushes first, or saving with the introduction open would have
written a stale reasoning and silently dropped everything just typed.

`save_tier_list` was **dropped before being recreated**, for a new reason worth
recording: adding a parameter with a default does *not* replace a function, it
creates an **overload** — and PostgREST resolves by named arguments, so a
four-argument call would have become ambiguous between the two. Same family as
the 42P13 return-type trap, third variant.

##### A bug that shipped in PR #81 and was invisible for a day

`js/description.js` was never loaded on the tier list page, so
`generateHTMLForBlocks` did not exist there — **the reasoning section could
never have rendered at all.**

Thirteen tests missed it because every fixture had `reasoning: []`. An empty
array takes the same branch as a missing renderer, so the tests agreed with the
bug. Found only because the new introduction used the same function and failed
loudly on a fixture that had content.

The lesson is narrower than "test more": **a fixture that is empty cannot
distinguish "renders nothing" from "cannot render".** Both new regression tests
supply real content, and one asserts the function exists on the page at all.

##### The storefront, which four PRs of foundation work forgot

Owner, 2026-08-14: *"We did upgrade the logic and rework the foundation, but we
kinda forget about the storefront."* Two things on the page's own chrome:

- **The edit button routed to `edit.html`** — the contributor editor, which
  edits the `page_data` row holding the retired 22-tab shape. It opened the
  wrong editor on data nothing renders any more. Now points at
  `tier-editor.html`, and `initTabEditorButtons` is no longer called on this
  page at all: it also injected a HISTORY button aimed at `history.html`, which
  browses the same dead row. The per-move changelog on the page is the history
  now.

  Deliberately **not** hidden from readers without a list. `tier-editor.html`
  says plainly that none is assigned to them, which is a better answer than a
  button that is missing for reasons nobody explains.

- **The sidebar header ran off the right edge of the window.** One
  `space-between` row, no wrapping, holding a title and however many buttons
  got injected beside it — two was already too many. Stacked now, so the
  buttons wrap under the title and a third costs nothing.

The overflow test measures against the viewport rather than a pixel value, so
it is not an OS-dependent assertion. Verified by reinstating the old
non-wrapping layout and watching it fail: **1354px of content in a 1280px
window**.

**Dropping `initTabEditorButtons` orphaned the mobile edit path**, and the
suite caught it. That button was a `<button>` with no handler of its own — the
shared wiring was the only thing that made it work — so removing the call left
a phone with no route to the editor at all. It is a plain link now, same
destination as the desktop one. The desktop button also kept its original id,
because `routing.spec.js` asserts elevated pages still offer an edit affordance
and that claim is still true; only the destination changed.

One test had to be re-pointed rather than fixed. `character-template.spec.js`
used the tier list page as the second of two surfaces for a **CSS** claim — a
per-page `<style>` block that duplicated shared rules and made these buttons
render differently on one page than another. That claim needs two pages
carrying the pair, and the tier list deliberately no longer does, so it now
uses a system page that still runs the shared wiring. The claim is unchanged;
only the sample is.
- [ ] **Resize the list:** bigger character icons, less spacing between them
- [ ] **Real versioning, changelog and reasoning.** Measured against production: `desc_data.tabs` is 22 entries of `{id, label, tiers, changelog}` — the changelog sits *inside* each tab beside the tiers, and there is no history at all. Each tab holds one current state. So "what changed, when, and why" has nowhere structured to live, and a reasoning field has nowhere to attach
- [x] **A tier list editor built on the existing editor**, with the list in the preview panel and draggable character icons

#### Shipped 2026-08-13 — the editor

`tier-editor.html`, its own page rather than a branch inside `edit.html`, and
the reason is the permission model rather than convenience. `edit.html` is the
contributor editor: everything written there becomes a pending revision a
reviewer approves. A tier list is **not reviewed** — it belongs to one named
person who publishes directly — so routing it through that page would mean a
second workflow living inside a file whose entire shape is the first one. The
two-pane layout, the block editor and the button families are all reused.

**The note rule is implemented as a diff, not a field.** The editor snapshots
where everyone started, and every difference becomes a row that has to be
explained before SAVE does anything. A changelog written afterwards is written
from memory; the notes worth having are the ones somebody wrote while they
still knew why.

Four consequences that fell out of doing it that way, each of which would have
been a bug if it had been a plain form:

- **Moving a character back where it started cancels the move**, and discards
  the half-typed note with it.
- **Renaming a tier is not a move.** The diff keys on tier name, so a rename
  would otherwise register as everyone in that tier moving — both wrong and a
  wall of notes to write. Renaming rewrites the baseline instead.
- **Removing a tier returns its occupants to unranked as explainable moves**,
  because removing a tier is a ranking decision about everybody in it.
- **A failed save keeps the moves and their notes.** Re-baselining before the
  write would silently discard the very moves it failed to record.

The unranked tray is **recomputed from the roster rather than stored**, so a
character added to the wiki after a list was written appears on the next edit
instead of being invisible forever. Boomcat is exactly that case today.

Drag-and-drop is written fresh rather than reused from `js/tierlist.js`: that
file's editor operates on the old `desc_data.tabs` model and is the
least-audited major file in the repo, so binding a new feature to it would mean
inheriting both its data model and its bugs. Pointer events, so mouse and touch
are one implementation. `window.tierEditorPlace` is the operation the drag
triggers, which also makes it drivable by tests without synthesising pointer
gestures.

`tests/tier-editor.spec.js`, 15 tests.
- [ ] **A tier list per assigned individual.** The owner assigns people; each keeps their own list, with their own reasoning and changelog, maintained as the game updates. From a discussion about objective versus subjective meta

**Note on the versioning item:** this is a data reshape of live content, so it needs the same treatment the matchup tier rename got — a migration, a renderer that reads both shapes during the transition, and `page_history` left alone. `js/tierlist.js` is also the least-audited major file in the repo, and its live-preview is intentionally disabled.

#### Settled 2026-08-13: the page becomes N lists, one per person

The 22-tab structure is **dropped**, not extended. Instead of one Overall plus 21 "vs character" tabs, the page holds one tier list per assigned individual — five people, five lists, credited by name, and the owner expands it by assigning more.

**Dropping the matchup tabs removes a duplicate rather than losing content.** Those 21 "vs <character>" tabs are a second ranking of the same information every character page already carries in its Matchups tab — the one v0.13 just widened to nine tiers. Two parallel sources for "how does X fare against Y" is exactly the drift the matchup vocabulary work existed to stop, and this retires the weaker of the two: the character page's version sits next to the frame data and the write-up, is edited by whoever knows that character, and is already in the review workflow.

- [x] **Seed the owner's list from the existing Overall tab** (owner, 2026-08-13). The current placements become the owner's own attributed list rather than being discarded, which also means the page is never empty on the day it ships. The 21 "vs character" tabs go; `page_history` retains them if anything is ever needed back
- [x] N lists, each owned by one assigned person, credited by name
- [ ] **Changelog: a note per move, required.** Every character that goes up or down gets a short note from that author at the time they move it. The editor has to detect placement changes and ask for the note, which makes it a validation rule rather than a text field somebody may ignore
- [ ] **Reasoning: a post under the changelog**, written long-form. This should reuse the existing block editor and `generateHTMLForBlocks` rather than growing a second content format — the site already has paragraphs, headings, images, callouts and the colour shortcodes, and a tier list author wants all of them
- [x] **A reader lands on nobody.** No list loads until a name is clicked, which is the point of the feature: there is no unattributed default to mistake for the wiki's own opinion
- [x] Show which lists were updated recently, so returning readers have a reason to pick one

#### Shipped 2026-08-13 — schema, seed and read path

Split from the editor deliberately: this half is the data model and the public
page, and it ships working with the owner's list already in it.

**Nothing was deleted.** `page_data.desc_data` for `tierlist` still holds all
22 tabs. The migration reads the Overall tab to seed and leaves the row alone,
so this is reversible by pointing the renderer back at it. `js/tierlist.js` is
untouched and still serves the admin preview and the existing editor — two
renderers, because there are still two data models and merging them would mean
one function with two permission stories.

**Why a table rather than more JSON, and it is not tidiness.**
`desc_data.tabs` held one current state per tab and no history, so "what
changed, when, and why" had nowhere to live. Per-person ownership makes that
decisive: **RLS cannot say "this person may edit their own entry inside this
JSON blob"**, only "this person may edit this row". The permission model forces
the shape.

**The character keys were rewritten**, with the owner's go-ahead. The old tabs
stored navigation display slugs (`Sus-Sister`, `Mangaka`, `Locust`) and leaned
on a normalizer that lowercased and stripped punctuation. Checked against live
data: all 21 matched — but three of them (`Locust`→`locust_guy`,
`Mangaka`→`aspiring_mangaka`, `Sus-Sister`→`crow_charmer`) differ from their
real `page_id` by more than punctuation and matched only because the display
slug happened to agree, which is not a property anyone was maintaining. Now
stored as `page_id`, the key every other table already uses. The migration maps
them by joining `site_pages` on a normalized `nav_id`, and **keeps an unmatched
key rather than dropping the placement** — losing one silently is worse than
showing one that needs fixing.

Also found while checking: **Boomcat is in the roster and in no tier**, having
been added after the list was last touched.

**The note-per-move rule is in the schema**, not only the editor: `note` is
`NOT NULL` with a length floor, and a change row whose `from_tier` equals its
`to_tier` is rejected outright. A changelog reading "moved Vessel from D to B"
and nothing else is exactly the artefact the rule exists to prevent, so the
editor cannot be the only thing standing in its way.

Two fine-tuning items from §8 came along, since this rebuilt the markup they
applied to: **bigger character icons** (60px → 78px) and **tighter spacing**
(0.5rem → 0.3rem). Scoped to `#tier-list-ui` so the editor's drag-and-drop
portraits keep the dimensions their hit-testing was built around.

`tests/certified-tier-lists.spec.js`, 13 tests. One caught a wrong assumption
in the test rather than the code: the three rewritten characters display their
proper names, and the expectation had been written from their slugs.

**A retired class name nearly came back.** The new stylesheet used `.tier-row`
and `.tier-label` — and `tierlist-live-dedupe.spec.js` exists partly to pin
that those exact names were **deleted as dead CSS in v0.11**, checking that a
bare `.tier-row` still computes to a transparent background. The new rule set
`background: var(--bg-secondary)`, which is precisely what the retired one set.
The test was right and the code was wrong: every new class is now namespaced
`ctl-`, which also rules out collisions with the old renderer still running on
`history.html`. Exactly the shared-CSS trap `CLAUDE.md` warns about, caught by
a test written for a different reason two versions ago.

Four tests in that spec covered the old renderer *through the tier list page*,
which no longer uses it. They were **re-pointed rather than deleted** — the CSS
specificity bug they guard (a bare `.tier-nav-overall-btn` losing to
`.btn-manga` on source order, rendering the tab 16px short) is still live for
`history.html` and the admin preview. They now build their own markup, so the
coverage no longer depends on which page happens to draw those elements.

One of them was **narrowed on purpose**: it used to assert the renderer emitted
exactly one inline custom property and no other inline styles. With the markup
now written by the test itself, that assertion would be checking its own
fixture. What survives is the half still about the stylesheet — the label's
background comes from `--tier-row-color` rather than a hardcoded rule.

**What this simplification buys.** The earlier concern was 22 tabs multiplied by N people — 110 lists at five assignees, most of which nobody would maintain. It is now N lists, and lazy loading falls out for free: one list is fetched when its name is clicked, rather than 22 tabs of placements on page load.

#### The permission model is still the new part

Attributing a ranking to a person is the right instinct for a technical wiki. Frame data is measurable and a tier list is opinion, and a single unattributed ranking quietly presents the second as the first — *"the wiki says this character is S tier"* is an argument nobody can win. A named list is a claim someone is accountable for, which is both more honest and less brigade-able than an official-looking consensus.

For reference, the structure being replaced, measured against production before the decision:

```
desc_data.tabs → 22 tabs
   overall            6 tiers,  1 changelog entry
   vs-honored-one     5 tiers,  1 changelog entry
   ... 20 more "vs <character>" tabs
tier      = { name, color, characters }
changelog = { date, notes }
```

**The permission model is new to this codebase.** Page access today is role-based — `page_permissions.required_role` gates a whole page, and anyone holding the role edits all of it. This needs per-row ownership: the assignee edits *their* list and not each other's, with admins above all of it. That is closer to the capability work than to the role work, and it is the part most likely to be underestimated.

### 4. Free Submit Tier List, with anti-brigading built in

- [x] Schema: one row per `(user_id, character)` vote, with `UNIQUE(user_id, character)` so a resubmission updates rather than stacks
- [x] Aggregate to a ranking, with the **sample size stored and displayed**
- [x] Account-age or contribution gate on voting
- [x] Rate limiting on submission
- [x] Show the **distribution**, not only the headline number
- [x] **Median, not mean** — owner's decision, 2026-08-13. Decided before the schema exists rather than after, because changing it later means recomputing every published ranking
- [x] The tool page exists and is empty: `tools/free-submit-tier-list/`, category `Community`

**What choosing the median commits to.** A median needs every individual vote kept, not a running total — you cannot compute one from a sum and a count the way you can a mean. So votes are stored per person and the ranking is derived, which is what the `UNIQUE(user_id, character)` row already implies. It also makes the distribution free: once the individual votes are there, showing the spread costs a query rather than a schema change.

The payoff is specific. A block of coordinated S-tier votes drags a mean immediately, while a median does not move until that block is more than half of everyone who voted. Against a Discord that can produce a few hundred votes in an afternoon, that is the difference between a ranking that reports the loudest group and one that reports the community.

#### Shipped 2026-08-14

`supabase/migrations/20260814000001_free_submit_tier_list.sql`, `js/tools/free_submit_tier_list.js`, `style/tools/free_submit_tier_list.css`, owner controls on `owner.html` + `js/owner-tier-lists.js`.

**The scale is the owner's own, read from production rather than invented.** S / A / B / C / D / F with the exact `hsl()` colours the Overall tab used, now seeded into a `free_submit_tiers` lookup table. Deliberately the same vocabulary as the certified lists: a community ranking that *disagrees* with a named one is interesting, and a community ranking measured on a different scale is not comparable at all. The lookup table exists because a median needs an **order** and a text column has none — `rank` is that order.

**Three medians, doing two jobs.** `percentile_disc(0.5)` picks the placement, and it returns an actual observed value — a tier somebody really voted. `percentile_cont(0.5)` is used *only* to order characters within the tier they landed in; without it a tier is an unordered heap, and with a mean there instead the ordering would reintroduce exactly the sensitivity to extreme votes that choosing a median removed. Every number is a median, so shifting any of them still takes more than half the electorate.

**The owner asked for a switch between the calculations, 2026-08-14, and the honest version of one is narrower than it sounds.** `percentile_cont` cannot place a character: for an even split it returns something like 4.5, a "B+" on nobody's ballot and on no scale this site uses. Rounding is worse — with the two middle votes two tiers apart it lands on a tier *nobody* chose. The real choice underneath is which of the two middle votes wins, which is `percentile_disc` taken ascending (the lower) or descending (the upper), and **both answers are always a real vote**. Stored as `free_submit_tie_break`, defaulting to `lower` — the cautious answer, where a character only moves up if more than half the voters put them there. It changes nothing for any character with an odd number of voters. The plain average is deliberately not an option.

**Within a tier, strongest on the left** — owner's call, 2026-08-14. Ordered by the continuous median, falling back to sample size so that of two characters the community rates identically, the better-attested one leads. Pinned by a test that hands the page its rankings worst-first, so passing cannot come from echoing the RPC's order, and falsified by reversing the comparator.

**There is no INSERT or UPDATE grant on the votes table at all.** Every write goes through `submit_tier_votes`. That is what makes the gate unskippable rather than advisory: a policy can express "this row is mine", but it cannot express "this account is old enough", which needs `auth.users`. DELETE *is* granted directly, because withdrawing a rating should work even when voting is closed — taking your opinion back must never be harder than giving it.

**`user_id` is `NOT NULL ON DELETE CASCADE`, which is the opposite of every other authored table here** and deliberately so. Elsewhere the column is nullable because `anonymize_user_by_email` hard-deletes the `auth.users` row and the *content* should outlive the account. A vote is not content: left behind as NULL it would be unupdatable by anybody, would inflate the sample with a ghost, and — because NULLs are never equal in a UNIQUE index — would **stop enforcing one vote per person** the moment two accounts were deleted.

**Raw votes are not public.** Only the voter and admins can read individual rows; the aggregate comes from a `SECURITY DEFINER` function that returns counts and no identities. "You rated my main F" is a harassment vector, and nothing legitimate needs the per-person data.

**The gate is asked about before the ballot is drawn.** `free_submit_eligibility()` returns the refusal as prose, so it reads as an explanation rather than an error — a rule discovered *after* rating twenty-two characters is a hostile way to enforce a perfectly reasonable one. It also fails **closed**: the settings are read into scalars with a `COALESCE` each rather than into a record, because a missing singleton would make every comparison NULL, and an `IF` on NULL does not fire — which would have let anybody vote.

**Unranked characters are shown, not hidden.** Below the vote floor a character appears in its own row labelled *"not enough votes to place yet — 10 needed"*. An absent character reads as an oversight; a labelled one reads as an invitation.

**The knobs are settings, not constants**, on `tier_page_settings` — which is what the previous migration said that table was for. When a brigade is under way the useful response is to close voting or raise the age floor from the owner tools within a minute, not to write a migration and wait for a release.

**Tool stylesheets are now a generator concern.** `style/tools/<page id>.css` is picked up the same way `js/tools/<page id>.js` already was, so a tool gets a real stylesheet instead of a `<style>` block written from JavaScript — the shape v0.13 spent six PRs removing from `editor.js`. The scan is exported as `scanToolAssets()` and shared with the suite, because *"the committed stubs match what the generator produces"* built its own empty sets and immediately called the new stub stale. Same trap `page-previews.json` sprang once already.

**36 tests.** The one that matters most: two characters with the **same median** and different spreads must not render the same chart — if they do, the distribution is decoration and a brigade is invisible. Falsified by ignoring the `ranked` flag, which failed exactly one test and no others. Character ids are read from `navigation.json` at runtime rather than written in, so adding a character never breaks the file.

Still unprobed: the constraint, the gate, the medians, and the grants. All four are in the live-probe list for the release PR's preview database.

### 5. Deleting media from the queue

**Owner's design, 2026-08-13**, and it is simpler than the quarantine-prefix sketch it replaces: deletion is a deliberate per-item action taken in the media queue, not an automatic consequence of flagging. Flagging still hides; deleting is what makes a file unreachable, and a person decides.

- [x] A delete button per item in the media queue, removing the storage object
- [x] Gated by **admin OR `can_delete_media`** — the third capability, following the v0.13 pattern
- [x] Whitelist `can_delete_media` in `set_user_capability`, which rejects unknown names by design
- [x] Confirmation naming the file, and saying plainly that it cannot be undone
- [x] Deleting must also settle the moderation row, so the queue does not keep listing a file that is gone
- [x] Warn when deleting something still referenced by a live page — the usage data is already there, and this is the one place it stops being advisory

#### Shipped 2026-08-13

**`can_delete_media` is deliberately narrower than `can_moderate`.** Moderation
comes free with reviewer; this does not, and falls back only to admin.
Reviewing a revision and destroying a file are different amounts of trust, and
this is the only irreversible action on the site.

**The confirmation is inline rather than a prompt**, because it has to show the
file name *and* its usage together, which a one-line text prompt cannot. Three
different warnings depending on what the usage data says:

| Usage | What it says |
|---|---|
| On live pages | names them, and that deleting breaks them |
| History/pending only | restoring one later would restore a broken reference |
| Nothing found | **"that is not the same as nothing using it"** |

That third wording is the important one. `media_usage()` finds references by
*extraction*, which can miss an unusual form; the garbage collector answers the
same question by conservative substring matching precisely so its mistakes fall
towards keeping files alive. "Nothing references it" has always meant "nothing
was found", and the person about to destroy a file is exactly who needs that
difference spelled out rather than implied.

**Storage first, then bookkeeping**, and the order is load-bearing. A failed
object delete leaves everything untouched. A failed *record* afterwards means
the file is genuinely gone — so the UI says "the file was deleted, but its
queue record could not be settled" rather than "failed", which would imply the
file survived and invite a second attempt at something that no longer exists.

**Deletions go in `moderation_log`** alongside post moderation rather than a
second log nobody remembers to read, with the usage summary recorded as the
snapshot — the only record of what the deleter was told before deciding.

#### The storage policy, and a live hole it uncovered

Storage rules for this project lived in the Supabase dashboard rather than in
migrations — invisible to code review, and **absent from every preview
branch**, since preview databases are built from the migrations. Asking to see
them turned up something worse than the thing being asked about.

The bucket had four policies, all for `authenticated`:

```
Auth Upload      INSERT   bucket_id = 'wiki-media'
Auth Update      UPDATE   bucket_id = 'wiki-media'
Auth Delete      DELETE   bucket_id = 'wiki-media'
Auth List Media  SELECT   bucket_id = 'wiki-media'
```

**DELETE was open to any signed-in account.** Not staff — anyone who had
registered could have emptied the bucket, on a site about to be pointed at by a
1.4M-member Discord. Pre-existing, unrelated to this feature, and found only
because this feature made someone look.

**It also made the first draft of this migration useless.** It added a *second,
narrower* DELETE policy — and permissive policies are OR'd, so `Auth Delete`
would have gone on saying yes and `can_delete_media` would have shipped as pure
decoration: a checkbox in owner tools that controlled nothing while sitting
next to two that do.

So the existing policy is **replaced, not supplemented** — same name, so the
dashboard still shows one obvious DELETE rule instead of two that have to be
read together. And it is altered-if-present, created-if-absent, because
production has the policy and a preview branch does not: an unconditional
`ALTER` fails on the preview, an unconditional `CREATE` fails on production,
and neither half can be assumed.

Only DELETE is touched. INSERT and SELECT are how contributors upload and how
the editor lists the library, and both must stay open. **UPDATE is a separate
open question** — it permits renaming an object, which breaks every reference
to it — but nothing in the app uses it, so narrowing it belongs in its own
change rather than riding along.

Worth recording for next time: probing without a token got partway and no
further. Anon listing `wiki-media` returned `[]` despite 316 files, which
proved SELECT was scoped. The anon DELETE probe was **inconclusive** — storage
resolves the object before consulting the policy, so a missing file returns
`NoSuchKey` whatever the policy says. Reading the dashboard was the only way to
know, and the answer was not the one the encouraging probe suggested.

**Why this resolves the collector conflict:** the collector deletes only what nothing references, and this deletes what a person judged. They no longer race, because the queue's delete is manual and immediate rather than a rule the collector has to agree with.

### 6. Abuse resistance and the contributor leaderboard

From the plan's item 4, and the pieces that only become concrete once threads exist.

- [x] **Reporting.** Moderators removing posts they happen to see does not scale to 1.4M people. A report button is what turns moderation from patrolling into a queue — the same shape as the media queue already built

#### Shipped 2026-08-13

**There is deliberately no "incorrect" or "misinformation" category**, and this
is the one genuinely domain-specific decision in the feature. On a frame-data
wiki *"this is wrong"* is the most common complaint and the least actionable by
moderation — a thread is exactly where being wrong should be argued with.
Offering the category would fill the queue with *"this guy is wrong about
Sukuna's DP"* and train moderators to skim, which is how a real harassment
report gets missed. Reports are about **conduct**; corrections belong in a
reply or in a revision to the page.

The four that exist: spam, harassment, off topic, other.

**Anti-brigading, on the reports themselves.** A `UNIQUE` index on
`(target_id, reporter_id)` means twenty people organising against one post
produce twenty rows a moderator can see the shape of, while one person with a
script produces exactly one. The index cannot see somebody walking a thread
reporting *everything*, so a second limit caps it at 5 reports per 10 minutes.

**The reply is identical whether the report was new or a duplicate.** Telling
somebody "you already reported this" leaks that their earlier report exists and
has not been actioned, which invites them to try again from another account.

**Reporters cannot read their own reports back.** There is nothing in the row
they did not write, and a reporter who can watch the status learns exactly when
a moderator looked — useful for evading one.

**`report_count` is computed server-side** and shown as a badge. Three reports
on one post is a completely different situation from one, and it is the single
most useful signal in the queue; doing it client-side would be a query per row.

Acting on a report is **two writes in a deliberate order** — moderate the post,
then close the report. A failure between them leaves the report open over a
post already down, which is visible work to finish, rather than a closed report
over a post still on the page. The UI says "Post handled, but the report did
not close" rather than "failed", because "failed" would imply neither happened
and invite doing it twice.

Two harness bugs the tests caught, both of which meant a test was passing
without proving anything:

- `data-report-post` was on **both** the trigger button and the form, so the
  delegated click handler matched the form and re-opened it instead of
  submitting. The submit event never fired.
- The spec's `adminPrompt` stub used `window.__promptReply ?? 'a reason'`, and
  `null ?? x` returns `x` — so the "cancelling writes nothing" test was
  cancelling with a real reason and proving the opposite of its name.
- [x] **`viewer` must actually block posting.** The soft ban exists and means "signed in, can read, cannot submit". If threads do not honour it, the ban stops meaning anything the moment threads ship — enforced in the INSERT policy with `IS DISTINCT FROM`, never `<>`, because `get_my_role()` returns NULL for an ordinary contributor and `NULL <> 'viewer'` is NULL, which would have denied everyone
- [x] **Thread rate limiting is its own limit.** 20 seconds, in its own trigger
- [x] **Decide whether `bypass_cooldown` carries over.** **No** — owner's call, 2026-08-13. It was written for submissions, and the thread limit applies to everyone including admins, so no single compromised staff account can flood a page
- [ ] Contributor leaderboard

### 7. Things that will break if threads ship without them

- [x] **Reply notifications.** The notification system has existed since v0.8. Threads without it means nobody returns to a conversation they started, and the feature looks dead even when it is being used. Shipped with threads as a `SECURITY DEFINER` trigger, linking to `<page url>#post-<id>`; the URL is read from `site_pages` rather than reconstructed
- [ ] **Flagged-media deletion vs the garbage collector.** If flagging deletes the object, the collector's reference scan and the moderation table can disagree about a file that no longer exists. Decide which is authoritative before both are automated

### 8. Fine-tuning phase

The owner's list, 2026-08-13. Leads noted where this session already touched the code; none of them are verified against the actual symptom yet.

- [x] **One font and size for every column sub-heading.** Site Info, Gamemodes, Servers, Community and the rest should all be Finger-Paint at a medium size. They disagree today: v0.13 fixed the *sidebar* group headers (`.sidebar-nav-title.sidebar-group-header`), but the *column* sub-headings are a different class (`.column-subgroup-title`) and were never brought along
- [x] **Tighter spacing between the tab buttons** in those three columns, keeping enough room that the hover state does not clip
- [x] **Narrow the four sidebar buttons** (Overseer, Inbox, My Submissions, Account) to the width the same buttons get on system-type pages. The sub-categories added enough rows to push Ko-fi off screen again — the v0.13 spacing fix bought back three rows and the new columns spent them
- [x] **Reclaim editor space.** A control that collapses the navigation buttons at the top, and a way to dismiss the tip once it has been read
- [x] **Media queue preview on desktop** opens in the preview panel beside the queue, rather than cramped into the sidebar. **Mobile already works — leave it alone**
- [x] **Drop the duplicate mobile Edit/History pair.** Owner's item, 2026-08-14, straight after the drawer shipped: with the ToC reachable on a phone, the copies in the page body are a second set of the same two controls
- [x] **Roster filter dropdowns miss the custom styling.** Archetype and tier, on both the Main Dashboard and the Character Dashboard. Likely cause: `initializeMangaSelects` targets `select.editor-select`, and these are `#filter-archetype` / `#filter-tier` from `js/pagebuilder.js`
- [x] **A full visual colour picker in the editor**, owner's call 2026-08-13: a saturation/hue surface rather than a styled hex field, and picking a colour wraps the highlighted text in the tag the way the presets already do. Replaces `<input type="color">`, which is what summons the OS dialog over the wiki's own palette. It goes through the same `applyFormat('color', …)` path the preset swatches use, so the write side is already built and tested
- [x] **Upgrade `internalStyling.js`.** Works, but dated — **it did not "work"; it carried three live stored-XSS vectors and only ran on one page type in five**
- [x] **The mobile sticky bar.** Owner's item, 2026-08-14. The site name in Finger-Paint, and the two controls rewired: **the site name opens the left sidebar, the burger opens the ToC.** Having the bar link to the Main Dashboard *and* the sidebar it opens link there too is redundant, and it was spending the one control a phone reader had

#### Shipped 2026-08-14 — the dashboard chrome

Four of the owner's items, measured before anything was changed. They turned out to share one shape: **a rule stated twice, or stated once and then beaten by source order.**

**The column sub-headings, measured on the live page:**

| Column | Sub-headings | Class | Rendered as |
|---|---|---|---|
| Guides & Such | System Pages, Site Info, Guides | `.sidebar-master-title` | Finger-Paint 18.4px |
| Others | Gamemodes, Servers, Misc | `.column-subgroup-title` | **ui-monospace 10.88px** |
| Tools | Creators, Community | `.column-subgroup-title` | **ui-monospace 10.88px** |

Two classes doing one job in side-by-side columns. Fixed by making them **one declaration block with two selectors** rather than two matching blocks — `.column-subgroup-title` keeps only its own spacing, because the systems grid gets its separation from `.systems-grid-container`'s gap instead. All eight now measure Finger-Paint 18.4px.

**The button gap** went from a uniform 1.25rem to `0.65rem 0.9rem`. The floor is geometry, not taste: `.btn-manga-slanted:hover` moves the button `translate(-2px, -2px)` and grows its shadow to 4px, so it paints **6px past its resting box**; the column gap is kept larger because `skewX(-10deg)` also leans each button ~3px sideways. Measured row gap after: 10.4px.

**THE SIDEBAR DOCK WAS A REAL BUG, not a preference.** `.dock-action-btn` (Layout.css) and `.btn-sys` (Buttons.css) both declared `padding` and `font-size` at identical `0-1-0` specificity — so the winner was decided by **which stylesheet the page loaded last**. Generated character stubs put Layout.css *after* Buttons.css; system stubs put it *before*. The same button measured **h45 on a character page and h34 on a system page**.

Fixed by deleting both declarations from `.dock-action-btn` rather than out-specifying them: `.btn-sys` then supplies them everywhere, which is simultaneously deterministic and the compact size the owner asked for. Ko-fi got the same treatment so it stops standing 8px taller than the four buttons it is stacked with, and nine hand-authored pages had the sizing **duplicated inline** on the Ko-fi anchor — replaced with the class. Verified uniform across nine pages and both stub types.

> **Carried finding, not fixed here:** the stylesheet *order* still differs between character and system stubs, so any other equal-specificity conflict between two shared stylesheets resolves differently by page type. Normalising the order is its own change with its own visual diff, and it does not belong inside a fine-tuning item. Added to §11.

**The roster filters** were the only dropdowns on the site without the `editor-select` marker `initializeMangaSelects` looks for, so they alone summoned the OS dropdown over the wiki's palette. Opting them in was **not sufficient**: the widget's stylesheet lived in `editor.css`, which the Character Dashboard has no business loading, so the wrapper would have rendered unstyled beside a still-visible native select. The `.manga-*` rules moved to `Forms.css` — every `editor.css` consumer already loads it — and `characters/index.html` gained the link.

Deliberately **not** moved with them: the `.editor-select` base rule. `editor.css` declares it twice, and pulling the later copy into a file that loads *first* would have silently handed the win to the earlier one — the same trap, one file over.

**7 tests**, asserting agreement between two rendered things rather than pinned pixels: "these two headings match" survives a future resize and "this is 18.4px" does not. The dock test compares a character page against a system page, which is the actual claim — a pinned height would pass just as happily with the bug restored on both. One drives the new dropdown and asserts the roster actually filters, because a dropdown that renders and does not filter is worse than the native one it replaced.

The homepage visual baseline was updated: the page is intentionally shorter now.

#### Shipped 2026-08-14 — the mobile top bar

**The table of contents did not exist on a phone.** `.local-sidebar-right` was `display: none` below 1024px — not collapsed, not behind a control, simply absent. On a wiki whose longest pages are precisely the ones a phone reader needs to jump around inside.

The bar had two controls and both pointed at navigation: a link to the Main Dashboard, and a burger that opened the left sidebar. The site was spending its only drawer control on the sidebar, and the sidebar it opened **already contains a link to the Main Dashboard** — so the link in the bar was the second route to the same place. Owner's call: give that control to the contents.

- **Site name → left drawer.** Now a `<button>`, not an `<a>`: it performs an action rather than going somewhere, and a screen reader should say which.
- **Burger → right drawer (the ToC).** Mirrors the left drawer exactly, from the opposite edge.
- **The mapping is spatial on purpose.** The control on the left opens the drawer on the left. Anything else is a coin toss for a thumb.
- **Finger-Paint on the site name**, which had been inheriting the body font — the one piece of branding on a phone was the only place the wiki's own typeface was missing.

**Five pages carry the bar and have no contents at all** (404, blog index, privacy policy, recent changes, submissions). The burger is hidden there rather than opening an empty drawer; the left drawer is still reachable from the site name, so nothing is stranded.

Two details that would have been bugs:

**`position: fixed` is load-bearing.** The closed ToC is parked off the right edge with `translateX(100%)`. Fixed boxes are outside the document's scrollable overflow region, so that costs nothing — `absolute` or `static` would have handed every phone page a sideways scrollbar. Measured: `scrollWidth - innerWidth === 0`.

**Following a contents link closes the drawer.** Without it the reader lands on the heading they picked with the drawer still covering it, which reads as the tap having done nothing.

The markup change touched 14 files plus the router; it is identical in all of them, so it was applied mechanically rather than by hand.

**8 tests** on a real 390×844 viewport, every one driving a real control. Falsified by pointing the burger back at the left drawer: 5 of the 8 failed. One guards the desktop — the contents must stay a column beside the page, not a fixed drawer sliding over it.

#### Shipped 2026-08-14 — the duplicate mobile buttons, and where a clip opens

Two items batched, at the owner's request.

**The mobile Edit/History pair is gone.** It existed only because the sidebar holding the real pair was `display: none` below 1024px. That sidebar is a drawer now and carries the pair with it, so the copies in the page body were a second set of the same two controls — a tab row wide on the narrowest screen the site serves, and one tap further away than the drawer they now sit in.

Removed from four places (`page_router.js` × 2, the character Template, the tier list page), along with `initTabEditorButtons`' entire mobile branch and the `.tab-editor-btn-mobile` / `#mobile-btn-group` rules.

**Four tests went with them**, and that is the right outcome rather than a loss. They guarded a real historical bug — `initTabEditorButtons` cloned `style.cssText` onto the History button, carrying a stale inline `display: none` that hid it on every system page but one — plus the phantom-duplicate follow-on at desktop width. Both were fixed by sharing one class, and now the buttons do not exist at all. **Tests for DOM that is gone assert nothing.** What replaces them is the claim that still bites: they do not come back, the stylesheet loses the rules too, and *the drawer carries the pair* — asserted in `mobile-drawers.spec.js`, because the drawer is now the only route on a phone and a regression there loses both controls outright with nothing to fall back to.

One thing the removal nearly cost: the tier list page's back-row was a `display: flex` container holding the link and the (desktop-hidden) button. Dropping the button and the flex made the page **7px taller** — `visual.spec.js` caught it. The container stayed; the baseline is byte-identical.

**A clip now opens in the main pane.** A reviewer deciding whether a video belongs on the wiki was judging it inside a queue row — the narrowest column on the page, at roughly a third of the available width. It opens in the pane a revision preview uses instead, because that is the part of the screen with room to look at something.

- **The phone keeps the inline behaviour.** The owner's instruction was explicit that mobile already works, and there is no second pane there to move it to.
- The 900px split is asked at **click time**, not at load, so rotating a tablet gets the arrangement that fits the width it now has.
- **Closing empties the panel rather than hiding it.** A hidden `<video>` keeps playing, and a reviewer hearing audio from a clip they closed has no way to find it.
- Opening a revision closes the clip and vice versa. One pane, one thing in it.

**Six tests.** The one that carries the claim measures the panel against the queue column and requires it to be **wider** — "the panel exists" would pass with the clip still crammed into the row. Falsified by forcing the pane off: 4 of 12 failed.

#### Shipped 2026-08-14 — reclaiming editor space

**Owner's decision when asked, 2026-08-14:** collapse the *action row and title*, not the tab strip. You collapse the chrome to get room for writing, and you still switch tabs while writing. Both preferences remembered.

Measured, which is the only thing that makes this a feature rather than a class:

| | Editing surface starts at |
|---|---|
| As it was | **358px** |
| Header collapsed | **244px** |
| Tip dismissed too | **167px** |

**191px back**, on a pane whose whole job is to hold text.

**Submit is deliberately exempt from the collapse.** It is the one control you cannot make somebody expand a panel to reach — a contributor who has finished writing should never have to hunt for it. Cancel, Drafts, Sync/Save, Diff View and HUB all fold away; Submit moves onto the collapsed row.

**The header's inline styles had to come out to do this** — a class cannot fold away what an inline style is holding open. They became `.editor-header-top`, `.editor-header-titles`, `.editor-header-side` and friends, and the new rules are scoped *through those containers* rather than styling bare `.editor-title` / `.editor-subtitle`. Those two class names are also on admin.html's heading, where `.admin-title` supplies the look at the **same 0-1-0 specificity** — a bare rule here would have been settled by which stylesheet that page loads last. Exactly the trap the sidebar dock fell into two days earlier, avoided by knowing about it.

That the `edit-logged-out` visual baseline still matches byte for byte is the evidence the inline-to-class move was faithful.

**Both preferences live in `localStorage`**, per-browser rather than per-account on purpose: this is a preference about a screen, not a fact about a person, and storing it server-side would mean a round trip before the editor could draw itself. Reads and writes are wrapped, because `localStorage` **throws outright** in Safari's private mode and with site data blocked.

**7 tests.** They measure the space rather than the class — "the class was applied" would pass with a collapsed header that gave nothing back. One pins that the tab strip is *not* part of the collapse, and one that expanding again is remembered too, which a one-way write would miss.

The storage-failure test was rewritten once. Blocking `localStorage` from boot also breaks the **drafts** module, which throws a modal over the button being tested — so it tested the wrong module's resilience and timed out on an intercepted click. It now breaks storage after load and calls the setters directly, which is the actual claim.

#### Shipped 2026-08-14 — `internalstyling.js`, and what "dated" was hiding

**The item said "works, but dated". It did not work.** Scoping it turned up three stored-XSS vectors live on the public site, a term list that had silently drifted from its source, two rendering bugs that had never worked, and the fact that the engine only ran on **one page type in five**.

##### The security half

Probed against a running page rather than reasoned about — the vectors were confirmed executing before anything was changed:

| Contributor text | What it produced |
|---|---|
| `[url=javascript:alert(1)]click[/url]` | `<a href="javascript:alert(1)">`, runs on click |
| `[url=" onmouseover="…]hover[/url]` | a **real `onmouseover` handler** on the anchor |
| `[color=red" onmouseover="…]x[/color]` | the same break-out, one attribute over |

The engine reads a block's `innerHTML`, string-replaces over it, and writes it back — an innerHTML sink whose every interpolated value is contributor-submitted. A double quote in a **text node** reads back out of `innerHTML` as a plain quote, which is how it reached an attribute.

**A reviewer could not have caught this.** The submission reads as the literal text `[url=...]`; the handler only exists after this file runs.

Closed with three gates — `escAttr`, `safeUrl`, `safeColor` — and anything failing them renders as **plain text rather than markup**. Losing a colour is a rendering nit. `rel="noopener noreferrer"` was added at the same time, since `target="_blank"` without it hands the opened page a handle back to the wiki.

##### The drift

The frame-term list was a hardcoded second copy of `FRAME_COLOR_LABELS`, and the copies had separated. **All six overlay-window colours** — Reverse Hitcancel and the five I-Frame kinds — had existed in `WINDOW_COLORS` and in the frame-data legend for versions and were **never highlighted in prose**, because nobody adds a tenth line to a list that looks complete. It is derived now, so the next token added is highlighted the day it lands.

Two bugs fell out of the rewrite, both of which had never worked:

- **"Extended Recovery"** rendered as a plain "Extended" plus a recovery-coloured "Recovery". The old code ran a replace *per term* in list order, so "Recovery" matched inside it first — and the later rule then found a `<span>` in the middle of its own phrase and never matched at all.
- **"5-8f"**, the normal way to write an active window, rendered as a plain "5" and a **red, disadvantage-coloured "-8f"**.

Both are the same root cause and have the same fix: **one combined pattern, one pass**. A single global replace never re-scans its own output, so first-match alternation with the longest terms first settles it. The `(?![^<]*>)` guard does not help here — it only protects attribute text, and `</` satisfies it immediately.

##### It ran on one page type in five

Only the **character** stub loaded `js/internalstyling.js`. System, gallery, tool and tier-list pages never did — so a contributor writing `[b]bold[/b]` on a guide page, in the same editor with the same shortcode buttons, saw the literal brackets. Added to `systemStub`, `mediaStub` and the tier list. Deliberately after the injection fix, not before.

##### The four additions (owner's list)

- **Character aliases**, owner-supplied. A separate `CHARACTER_ALIASES` map rather than more keys in `CHARACTER_COLORS` — "Mangaka" used to live in the colour dictionary with a comment explaining it was a text fallback, which meant anything iterating the roster saw a character that does not exist. A colour dictionary describes the roster; this describes how people write about it.
- **Character mentions link to their pages.** A separate, idempotent DOM pass over the spans step B leaves behind, *not* another string replacement — `applyInternalStyling` is called synchronously from a dozen places and its output is not re-enterable, so linking cannot wait inside it for a fetch. Skips mentions already inside an anchor, and skips the character's own page.
- **`[noauto]…[/noauto]`**, because auto-highlighting matches ordinary English and the alias list adds Yuki, Todo, Charles and one outright sentence fragment.
- **`[kbd]`**, a real `<kbd>` so the meaning reaches a screen reader.

**22 tests.** Falsified by defeating the three gates: **6 of 11 failed**, and the six were exactly the injection ones. One test exists solely to catch the failure mode a security fix invites — refuse everything and pass every other test.

One bug the tests caught in my own work: the term pattern accepts "Bullet Iframes" as well as "Bullet I-Frames", but the lookup key kept the hyphen as a space — so the regex matched, the map missed, and the term rendered unstyled while every regex-level assertion still passed.

#### Shipped 2026-08-15 — the colour picker, and the toolbar catching up

**The fine-tuning phase (§8) is complete.**

`<input type="color">` is gone. It opened the **operating system's** colour dialog on top of the wiki — a different palette, different conventions, and on some platforms a modal that steals the text selection the toolbar is about to wrap. In its place: a saturation/brightness surface and a hue slider, drawn with CSS gradients rather than a canvas, because the maths is four lines and a canvas would be a second way to draw the same square. Output goes through the same `applyFormat('color', hex)` path the preset swatches already used, so the write side was built and tested.

Typing a hex still works and **drives the surface** rather than bypassing it — one source of truth, not two that can disagree. The hue slider takes arrow keys, because a drag surface with no keyboard is unreachable without a pointer.

**Also shipped: toolbar buttons for `[kbd]` and `[noauto]`.** Both shipped with the shortcode engine and had no way to be typed except by hand, which is the same as not existing for most contributors. The toolbar now offers every shortcode the engine renders — a test asserts exactly that, so the next one added cannot be forgotten.

##### Three bugs found by building it, all in the popup

1. **The picker sat under the scroll fold.** The popup caps at 320px and the ~45 preset swatches already fill it, so the surface and the USE button were below the line — opening the picker showed nothing but swatches. Fixed by scrolling *only* the swatches and pinning the picker.
2. **Raising the cap to 420px made the popup unclickable.** It opens upward, and at that height its top reached into the editor's **tab strip**, which paints over it — the upper swatches resolved as "visible and enabled" and then swallowed every click. `color-presets.spec.js` caught it. The ceiling went back to 320px and the surface got shorter instead; pinning the picker is what makes the smaller height affordable.
3. **`setPointerCapture` throws** for an id that is not an active pointer, and it ran *before* the colour was applied — so the throw aborted the handler and a click did nothing but log an exception. Capture is attempted after the work now, and in a `try`.

A fourth was mine and cosmetic: the "Custom" heading reused `.format-color-popup-label`, which labels a **swatch group**. There are exactly three of those, and a fourth made the picker read as a palette — an existing test said so.

**10 tests**, driving real mouse and keyboard. Synthetic `PointerEvent`s were tried first and hid the capture bug; they also silently failed to set a selection at all, because the editor saves it on `mouseup`/`keyup` inside `#block-list` and a programmatic `setSelectionRange` fires neither — the toolbar wrapped an empty string at position 0 and the test read it as a formatting bug.

### 9. Bug fixes phase

The owner's list, 2026-08-13.

- [x] **The Emotes page has no edit button**, so nobody can contribute to it. Gallery is the newest page type and the least exercised — this is the first gallery page anyone has tried to use
- [x] **Blocks render unstyled off a character page.** Owner-reported 2026-08-15: *"the tools pages are missing some styling sheets, so some block or elements won't show up"*
- [x] **The reviewer cannot see character-state changes in the preview.** A revision that adds a new state, or edits inside one, renders as unchanged everywhere except Diff View. The same gap covers base-only characters, whose Ultimate tab is not rendered in the preview either

**The second one is more serious than a rendering bug.** A reviewer approving what they cannot see is approving blind, and Diff View is a fallback for verifying detail, not a substitute for seeing the page. That is the same family as the v0.12 P0 in the merge compiler: not data destroyed outright, but a review process that quietly stops being a review. It belongs at the front of this phase.

Character states shipped in v0.12 and the admin preview was never taught about them — `js/admin-preview.js` builds the character page without the mode toggle `js/character_modes.js` injects, and `ultimateAtk` is a tab the preview does not know exists.


#### Fixed 2026-08-15 — two escaping and styling bugs

**`cp.topic` and `cp.importance`, unescaped into `innerHTML`.** The counterplay card is the same shape as the matchup card twenty lines above it in `js/description.js` — that one was escaped in v0.13 and this one was deliberately left alone to keep that PR to its two items. Both values are contributor-submitted, so `<img src=x onerror=...>` as a counterplay topic executed on the live page, and a quote in either field opened a new attribute.

`impColor` is still interpolated unescaped, and that is correct rather than an oversight: it comes from a hardcoded map with a fixed fallback, so `cp.importance` *selects* a colour rather than supplying one. A test pins that an unknown importance falls back to grey, so a future change cannot quietly make the value reach the attribute.

**5 tests**, falsified against the pre-fix code: 3 of 5 failed.

**Blocks rendered unstyled off a character page** — owner-reported, and the cause was a block split across two stylesheets. `.combo-container` and `.combo-meta-group` were in `Layout.css`, which every page loads; `.combo-node`, `.combo-arrow`, `.combo-damage` and `.combo-note` were in `FrameData.css`, which **only character pages load**. So a combo authored on a guide, a gallery or a tool page rendered as a plain row of words. `.strategy-title` had the same split.

All five moved to `Layout.css`, beside the container that was already there. They are authored-**block** styles — `generateHTMLForBlocks` emits them and every page type that renders blocks needs them. Frame data is a different thing that happens to also show combos.

**The test for this re-runs the audit rather than checking the five classes that happened to be wrong**: it derives the universally-loaded stylesheet set from the generated stubs, extracts every class the block renderer emits, and fails on any that only a page-type-specific sheet defines. The next block whose styling drifts fails here instead of being reported months later.

One thing that comparison nearly got wrong: `.strategy-title` is `var(--accent-blue)`, and `js/site_meta.js` re-points that per character — so a character page renders it in that character's colour and a tool page in the site default. Asserting equality there would have pinned correct behaviour as a bug. Colour is excluded from the signature and asserted separately.

**A regression from PR #90, reported the same day.** The Matchups table of contents indexed as a column of bare "vs." entries with no opponent names.

`refreshTOC` strips `a, button` out of a heading before reading its label, so the dashboards' nested *"View More ➔"* control does not end up in the entry. That was fine until v0.14 taught `js/internalstyling.js` to wrap character mentions in a link: every matchup heading became `vs. <a>Honored One</a>`, and the strip took the name with it.

**The owner's two exceptions are what identified it.** "vs. Ten Shadows" survived on Ten Shadows' own page because `linkCharacterMentions` deliberately skips the character whose page it is; "vs. Disaster Plant" survived everywhere because it is a typo for *Disaster Plants* and matches no character, so it was never wrapped. Both are precisely the cases that produce no anchor.

The rule is now controls, not anchors: `button, a[class*="btn-"]`. **A link the author wrote into a heading is prose and its text belongs in the label; a button-styled one is furniture and does not.** That also fixes a heading containing a `[url=]` link, which had been losing its text for the same reason without anybody noticing.

The test sits beside the control-stripping one it is the other half of, and it asserts the mentions are *actually linked* first — otherwise it would pass for the wrong reason, since it is the stripping of those links that broke it. Falsified by restoring `a, button`.

> Separately: **"Disaster Plant" is a data typo** on every page that carries that matchup. It is content rather than code, so it is the owner's to fix in the editor.

#### Fixed 2026-08-15 — the Emotes page had no way in

One line in `js/page_router.js`:

```js
const hasToc = route.pageType !== 'gallery';
...
${hasToc ? rightSidebar(...) : ''}
```

**The reasoning was right and the consequence was not.** A gallery genuinely has no headings to index — its own search box is how you find something in it — so it should not get a table of contents. But `#btn-edit-current-tab` lives inside that same aside, so dropping the list dropped the buttons: `initTabEditorButtons` ran, found nothing, and did nothing.

That aside is two things at once, and v0.14 made it worse without noticing. Removing the duplicate mobile Edit/History pair was correct — the drawer carries the real one — but it also made this sidebar the **only** place those buttons exist. A page type that omits it has no editing pathway at all, on any device.

`rightSidebar(label, withToc)` now separates them: a gallery gets the header and the buttons under the heading *"This page"*, and no contents list. The original decision is kept and asserted.

**7 tests.** One clicks the button and follows it to `edit.html`, because a visible button that leads nowhere is the same bug wearing a hat. One drives a phone, since the drawer is the only route there. The last one is the general claim — **every generated page type offers a way to edit it** — so the next page type cannot ship without one.

#### Fixed, 2026-08-13

Measured before building, against a revision that completely rewrites a skill
inside an ultimate state:

```
changedTabs: []      popup: none      marked tabs: none      state toggle: absent
```

No signal of any kind. The reviewer sees a page identical to the live one and
is told nothing — the whole bug in one line.

Four parts, in `js/admin-modes.js` and the three files that had to learn about
states:

- **A state toggle in the preview** (`#preview-mode-bar`), the same
  `.character-mode-chip` control the live page and the editor already use, so
  a reviewer picks a state with a control they know. Hidden for the pages that
  declare no states, which is most of them.
- **The Ultimate tab**, injected when either version has `ultimateAtk` moves.
  Deliberately unlike the live page, which also shows it empty for any
  registry-marked base-only character: a reviewer is not filling it in, and a
  permanently-empty tab in a review strip is one more place to check that never
  has anything in it.
- **Change detection narrowed to the state on screen.** `getTabData` compared
  an ultimate state's tabs against the base kit's, so a state edit read as a
  wholesale rewrite in one direction and as nothing at all in the other. Both
  sides now resolve through `resolveModeDesc` / `resolveModeFrame`.
- **A line in the popup naming the states that changed but are not on screen.**
  This is the part that actually closes the bug: markers can only describe the
  state being rendered, so something has to say "the edit is real, it is over
  there". The chips carry the same blue dot the tab strip uses.

Two things found on the way and fixed with it:

- **Intercept dropped the state.** `editCurrentTicket` appended `&tab=` but no
  `&mode=`, so a reviewer who switched to an ultimate state and intercepted
  landed on the base kit — and would have written their corrections into the
  wrong state. A mode-scoped ticket still overrides, being the stronger claim.
- **The preview reached into `modeData` directly** rather than through
  `resolveModeDesc`, losing the one documented exception: a state that has not
  overridden the portrait shows the character's. A state with no profile of its
  own rendered as a page with no character on it.

`tests/admin-preview-states.spec.js`, 8 tests. All 8 fail against the pre-fix
code.

Three more found by this project rather than reported, worth folding into the same phase:

- [x] `cp.importance` interpolated unescaped into `innerHTML` (`js/description.js`, counterplay block). Same class as the matchup ones fixed in v0.13, left alone then to keep that PR to its two items — **`cp.topic` was unescaped too**
- [~] ~40 `waitForTimeout` calls behind the v0.13 flake class. `retries: 2` made them survivable, not fixed — **42 → 21, with 4 kept deliberately**
- [ ] Suite worker scaling: 4x workers buys 1.44x, and 8 is slower than 4. Supabase ruled out at 3%; a naive Node test server was worse. Still unexplained

#### Moderator access to the Overseer, 2026-08-13

The owner's correction to how moderation was shipped:

> *"It is fine to let the moderator into the overseer panel to moderate — it is
> THE staff page. But they can't see the submission queue and media queue,
> cause it is not their job. People with the moderate perk can see the button
> to navigate to the admin panel, there is no need to create a new moderator
> role."*

Still a capability and not a role, so `UNIQUE(user_id)` on `user_roles` is
untouched. `admin.html`'s gate now admits admin, reviewer **or** `can_moderate`;
a moderator-only account gets the report queue and nothing else.

**The two gates were changed together, on purpose.** `js/pagebuilder.js`
already carried a comment about the time they drifted — it listed roles
`admin.html` did not accept, so those users got a working-looking OVERSEER
button that dead-ended at ACCESS DENIED. `tests/moderator-access.spec.js` now
drives *both* real code paths against the same four inputs in one test, rather
than asserting them separately, because separate assertions are exactly what
let them drift last time. Verified by deliberately re-introducing the drift and
confirming the test fails.

**The queues are hidden, not removed.** Five files call `getElementById` on
those containers without guarding; deleting them would turn every one of those
into a null dereference for exactly one class of user — the one nobody tests by
hand. Hidden before anything loads, in JS rather than CSS, so no queue flashes
into view and disappears.

**This is presentation, not security.** `pending_revisions` and the media
tables have their own RLS and a moderator-only account cannot read them
whatever the page does. The claim is "not shown a job that is not theirs", not
"prevented from doing it" — worth stating, because a hidden div is not an
access control and should never be mistaken for one.

The report queue also loads on arrival for a moderator, rather than waiting for
a Load press: for them it is the entire page.

#### Fixed on the way: a latent race in `page-skeleton-parity`

Found while verifying reporting, and **confirmed pre-existing** — reproduced
3/3 on `next-update` with none of the reporting work applied, failing on a
different two or three characters each run.

`loadPageDescriptions` adds `vessel-content space-y-6` to `#tab-overview`,
`#tab-matchups` and `#tab-counterplay` once the page's data arrives. The
character boot fires it without awaiting, so whether it had run by the time the
spec snapshotted class lists depended on how fast a network response came back.
It asserted exact equality against the router's classes, so it lost that race
at random. Almost certainly latent for a long time and made reliable by the
suite growing — see the runtime measurements in §11, where CPU contention turns
out to stretch every navigation.

Ids and their order are still asserted exactly, and every router class is still
asserted present individually — including the load-bearing asymmetry where
`#tab-skills` gets `space-y-8` and `#tab-overview` gets plain `.tab-content`.
What is no longer asserted is the *absence* of classes the render layer
legitimately adds afterwards, which was never this spec's claim to make.

#### A local-only backend override, 2026-08-13

Owner's question, part-way through the tier list editor: *"Supabase database is
not updated with the new stuff yet. Do you think it is best if we make some
change for it to test things out, or we leave it to the release?"*

The honest state at that point: **five migrations, roughly eight RPCs and
fifteen policies, none of which had ever been executed by a real client.** Every
auth spec in this project mocks Supabase and never touches Postgres, so RLS
predicates, grant gaps, trigger ordering and RPC guards were all unverified.

**Branching proves a migration APPLIES. It does not prove the feature WORKS**,
and only the second claim matters on release day. Left alone, the first real
click on a release's worth of policies would have happened on production.

So `js/site_utils.js` can now point a locally served copy of the site at
another Supabase project — in practice a branching preview database. Guarded
twice, and both guards are deliberate:

1. **Hostname must be localhost or loopback.** `dogslamloop.com` cannot satisfy
   this whatever is in storage, and the function returns the production target
   *before ever reading storage* on a remote host.
2. **A localStorage key must be set by hand.** Visiting a local copy does not
   opt you in.

Neither guard depends on a build flag or an environment variable, because this
site has neither — it is static files served as-is, so the check has to be
something true of the running page.

A malformed override falls back to production rather than leaving the page with
no backend at all: a page with no client is worse than one on the wrong client,
because every feature silently does nothing and nothing says why. Only
`https://<ref>.supabase.co` is accepted, and an active override warns on every
single page load — testing against a preview while believing it is production,
or the reverse, is the one way this helps nobody.

`tests/supabase-override.spec.js`, 6 tests, most of them about the guards
rather than the feature. An override that could activate on the live site would
let anything able to write localStorage point real readers at somebody else's
database.

##### It drew blood on its first day, 2026-08-14

The owner created a preview branch, looked around, and **deleted it** — leaving
the override in localStorage pointing at a project that no longer existed.
Every request failed, login stopped working, and **nothing on screen said why**.

Two things were wrong, and only one of them was obvious.

**The console warning was useless.** It was written for somebody already
suspicious, and the entire failure mode of this feature is *not* being
suspicious of a page that looks completely normal. Replaced with an on-page
banner naming the target project and carrying a one-click **USE PRODUCTION**
button, so recovery needs no knowledge of the storage key or the console.

**`index.html` is the only page on the site with a CSP**, and its `connect-src`
pins the production project — so the override can never work there whatever the
client was built with. That was the wall of errors in the report. Deliberately
*not* fixed by widening the CSP: `connect-src` pinning is real defence in depth
on the most-visited page, and weakening production to enable a dev convenience
is the wrong trade. The banner names the problem on that page instead, and the
override works on every other page, which is all of the surface worth testing.

**The tests missed it because they tested configuration, not reachability.**
They asserted the client was *built with* the right URL — which was true, and
useless, since every request was then blocked or refused. A mock cannot notice
that a real network call would fail. The banner tests are behavioural instead:
the banner appears, says which project, warns on the page whose CSP blocks it,
and clears on click.

One of those tests failed first time for a harness reason worth recording:
`addInitScript` runs on **every** document load, so it re-wrote the override
during the reload and the banner could never be observed clearing itself. The
harness would have hidden the exact behaviour under test.

**Rejected: applying the migrations to production early.** It would work and
nothing would break — the tables sit unused until v0.14 ships — but production
would then match no git ref, which is the drift the whole migration pipeline
exists to prevent.

### 9b. The live probe suite

`scripts/probe-release.js`, written 2026-08-15.

**Playwright mocks Supabase in every auth spec — it never touches real Postgres.** So RLS, table grants and the caller checks inside `SECURITY DEFINER` functions are the one part of this site the suite cannot reach. v0.14 shipped **8 migrations, ~20 functions and a dozen policies**, and not one of those gates has been proven either to work or to refuse.

The probe runs a three-case matrix per RPC and per table:

| As | Must | Why that case |
|---|---|---|
| **anon** | fail, except where deliberately public | Catches a function created without `REVOKE`. Postgres grants `EXECUTE` to `PUBLIC` on creation, so **every new RPC starts exposed** — exactly how the 2026-08-07 escalation happened |
| **user** (signed in, no role) | fail with `42501` on anything staff-only | The client-side RBAC gate is decoration; the REST endpoint is reachable directly |
| **admin** | **succeed** | The case that matters most, and the one an over-tightened policy breaks invisibly, because nothing else on the site notices |

Three decisions worth recording:

**It never wants the service-role key.** That key bypasses RLS entirely, so a probe using it would report success for precisely the rules it is meant to test.

**A missing row is a pass.** `P0002`, `22023` and `53400` mean the caller *got in* and the function then complained about the data — which is what "may call" looks like. That is why every probe uses ids that do not exist.

**`--include-writes` is refused against production** by a hard check on the project ref. These probes create tier lists, cast votes, grant roles and write moderation rows; that is safe on a disposable preview branch and is not safe on the live wiki.

**Skipped probes are reported as skipped, never as passes** — the whole failure this file exists to prevent is a green result that measured nothing.

A dry run against production returns `PGRST205` for 14 of 15 probes, which is the documented normal state before the release merges rather than fourteen findings. The script detects that case and says so once.

### 9c. The `waitForTimeout` cleanup — half done, deliberately

**42 calls to 21**, concentrated where they were thickest: `owner-pages` (9→1), `owner-personnel` (8→2), `owner-account-deletion` (4→1), `admin-rbac-retry` (4→0).

Three replacements, by what the test was actually waiting for:

| The wait was for | Replaced with |
|---|---|
| An async write landing in a mock's array | `expect.poll` on that array's length |
| A DOM update after a fill or a click | `expect.poll` on the rendered content |
| Something a Playwright assertion already waits for | **nothing** — `toContainText` and `toBeAttached` auto-wait, so the sleep only delayed the failure |

The third is the most common and the most embarrassing: several sleeps sat immediately before an assertion that would have waited on its own.

**`admin-rbac-retry` is the interesting one.** Its four waits were 1200ms each, out-guessing an RBAC gate that retries once over ~600ms. They now wait for the thing that *proves* the gate finished — an attached queue container — and then assert the denial screen is absent. That ordering matters: the negative check now runs after the gate has settled rather than merely 1200ms later, so it means something it did not mean before.

**Four calls are kept on purpose**, each labelled in place. They assert that *nothing ever happens* — a cancelled confirm writes nothing, a move at the boundary does nothing. There is no event for "still hasn't happened", and polling can only wait for something to appear. A bounded window is the honest tool for a negative.

**~17 remain**, one or two per file across fifteen files. They are the same three shapes and the work is mechanical; it was left rather than rushed at the end of a long session, because a sloppy conversion turns a flaky test into a wrong one. The flake class is materially reduced, not eliminated, and the entry above says so.

### 10. Watch list

Not scheduled. Things v0.13 surfaced that will come due on their own, recorded so they are recognised rather than rediscovered.

- **The sidebar will overflow again.** v0.13 bought back the rows the new `others/` pages cost by tightening spacing. The next batch needs the list to scroll independently so the buttons below it stay anchored — spacing cannot keep paying for this.
- **The colour picker is clamped horizontally, in JS.** A fourth palette row would make the panel taller than the pane, which is a different overflow than the one that was fixed.

### 11. Carried debt, not features

- [~] ~40 `waitForTimeout` calls across a dozen spec files — the class behind every v0.13 CI flake. `retries: 2` made them survivable, not fixed — **half done, see below**
- [ ] `cp.importance` interpolated unescaped into `innerHTML` in `js/description.js`
- [ ] Server-side garbage collection is now possible — but it must use reference **extraction**, not a `LIKE` join, which measured **14.7s** over 9 MB
- [ ] **Stylesheet order differs by page type.** Generated character stubs load `Layout.css` *last*; system stubs load it *second*. Any two shared stylesheets with an equal-specificity conflict therefore resolve **differently depending on which kind of page you are on** — found 2026-08-14 when the same sidebar button measured 45px tall on a character page and 34px on a system page. That instance is fixed at the source (the conflicting declarations are gone), but the ordering hazard remains and nothing detects the next one. Normalising the order is a real change with a real visual diff, and it wants its own PR with `visual.spec.js` run before and after — not a line inside a fine-tuning item

#### Suite runtime, measured 2026-08-13 — and the worker-scaling mystery, solved

Asked whether 710 tests is too many. Measured rather than guessed. Five
navigations of a real character page, idle machine:

| How the test navigates | Per navigation |
|---|---|
| `networkidle` + real CDNs — **what 311 of the 359 do** | **1474 ms** |
| `networkidle`, CDN scripts served locally | **768 ms** (−48%) |
| `domcontentloaded` + real CDNs | **301 ms** (−80%) |

Roughly one navigation per test, so ≈ 4.4 of the 6.9 minute run is *navigating*.
The assertions are nearly free. For contrast the 42 `waitForTimeout` calls
total **17.6 s** across the whole run — they are a flakiness problem, not a
speed problem, and the item above is about reliability rather than time.

**This explains §9's "still unexplained" worker scaling.** The first run of that
experiment happened while a full suite was saturating the CPU, and it showed
CDN-stubbing as *slower* — the opposite result. `networkidle` waits for 500 ms
of network silence, and under contention that silence takes longer to arrive,
so every navigation stretches. More workers → more contention → longer waits →
net loss. Supabase was ruled out back then at 3%; `networkidle` itself was
never the suspect.

**The count is not the problem and tests are not being deleted.** There is no
build step, no type checker and no staging environment, and a merge to `main`
deploys — the suite is the only thing between a typo and the live site.

Two fixes, neither urgent:

- [ ] A global route fixture serving `supabase-js` from a local copy and
      dropping KaTeX in tests. One file, no test changes, roughly halves the
      run. Owner's call 2026-08-13: **do it in the fine-tuning phase**, not now
- [ ] Retire `networkidle` in favour of `domcontentloaded` plus an explicit
      wait for the thing the test actually cares about. The larger lever and it
      *reduces* flake, but it is ~300 call sites each needing judgement — some
      genuinely need data to have arrived. File by file, as they are touched

---

## Anti-brigading, for the record

The Free Submit list averages every vote, and an average has no defence against volume. That is the whole vulnerability.

**Brigading** is a coordinated group voting together to move a result. One message in a 1.4M-member Discord — *"everyone go rate this character S tier"* — produces hundreds of votes in minutes, and the average moves to wherever they pushed it. The same effect comes from one determined person with alt accounts.

It matters here specifically because the ranking would carry the wiki's official endorsement. A number that says "the community rates this S" is worth something only if it reflects the community rather than whoever organised best that afternoon.

The defences, roughly in order of value:

1. **One vote per account, enforced in the database.** A `UNIQUE` constraint, not a client check — resubmitting updates your vote instead of adding another.
2. **A gate on who may vote.** Account age, or at least one approved contribution. Alt accounts made for a single vote are the cheapest attack and this is what makes them expensive.
3. **Publish the sample size.** "Averaged from 412 votes" lets a reader judge for themselves. A ranking with no vote count cannot be evaluated at all, which is why the plan called it meaningless.
4. **Median rather than a plain average** — chosen, 2026-08-13. A coordinated block of extreme votes drags a mean and barely moves a median. The single most effective change for the least work, and free at this stage because nothing is built yet.
5. **Show the distribution.** If opinion is genuinely split, a bar chart says so; an average silently reports the midpoint as consensus, which is a different claim entirely.
6. **Rate limiting**, so a script cannot submit faster than people can.

None of these stop a determined brigade outright. Together they make it expensive, visible, and much less rewarding — and 3 and 5 mean that even a successful brigade is legible to anyone looking, rather than laundered into a clean-looking number.
