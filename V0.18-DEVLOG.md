# v0.18 — devlog

**SCOPED 2026-09-08.** This file is the SPEC: design decisions are recorded here
*before* they are built, with data shapes and delta scopes worked out. Read the
whole of a section before building from it — v0.15's Combos tab was built to an
invented shape while the real one sat in the same file.

Source list: `devlogs/v0.18 Lists`, written by the owner. Everything below is
that list combined with the inventory carried since v0.16, scoped by the owner
on 2026-09-08.

v0.17 shipped 2026-09-03 as PR
[#156](https://github.com/Deptr-softwares/dogslamloop-wiki/pull/156), merge
commit `07eb647`. Two silent releases followed it (`devlogs/SILENT-1-DEVLOG.md`,
`devlogs/SILENT-2-DEVLOG.md`) and neither took a version, so the site still
reads Beta v0.17 and this is the next number.

---

## Scope — 19 items

The owner's list numbers the features; the carried inventory slots in as FT5 /
FT6 / FT8 without collision, because the new list stops at FT4 and FT7 shipped
in v0.16. **Do not resolve these numbers against `devlogs/V0.16-DEVLOG.md`'s own
fine-tuning headings** — that file has a different FT6, FT7 and FT8, all
shipped. The carried three are defined at `V0.16-DEVLOG.md:793-795`.

### Features

| | Item |
|---|---|
| **F1** | QA notes visible on the queue card, **before** the ticket is opened |
| **F3** | Ordering for Combo Lists |
| **F4** | Ordering for rows within a Combo List |
| **F5** | Anchors work on System-type pages |
| **F6** | First-visit notice modal — the editor |
| **F7** | First-visit notice modal — the Media Library |
| **F9** | Character Gallery tab *(carried from v0.16)* |

### Fine-tuning

| | Item |
|---|---|
| **FT1** | Move-input auto-colouring: white outline becomes black, colours brighter |
| **FT2** | Dark character names get a white outline **only on dark-coded pages** |
| **FT3** | Free Submit Tier List uses character portraits |
| **FT4** | Certified Tier List contributors switch between portraits and icons |
| **FT5** | Diff View renders the system-pages engine *(carried)* |
| **FT6** | Editing a system page renders only the section being edited *(carried)* |
| **FT8** | Stress-test the `internalStyling` rules for edge cases *(carried)* |

### Bugs

| | Item |
|---|---|
| **B1** | Reorder jitter — the moved item flashes in first place for a few frames |
| **B2** | **System page tab reorder is silently discarded** |

### Also

| | Item |
|---|---|
| **A1** | `CONTRIBUTING.md` *(carried; only a two-line README exists)* |
| **A2** | Refresh `visual.spec.js` baselines |
| **A3** | `.btn-sys` unscoped duplication |

---

## Deferred to v0.19, with reasons

Recorded here rather than dropped, because this project has lost an item to a
version split before — "Box-in a list" fell between v0.15's two lists and went
unassigned for weeks.

**F2 — Ctrl-click multi-select blocks, Ctrl+C / Ctrl+V.** The owner's own new
item, deferred by them on 2026-09-08. It was the largest thing on the list by
some distance and would have set the pace of the whole release. It is a
selection model over `js/editor-blocks.js`, not a keybinding.

**F8 — Media Library uploader credit.** Carried since v0.16 and now deferred a
**third** time. It is no longer blocked on anything: v0.17 shipped the person
record it was waiting for, so it is a join against `user_profiles` through
`get_public_profiles(uuid[])`, and `get_user_expert_pages(uuid)` is the shape to
copy. **Trigger: it goes in v0.19 unscoped-first, before new work is added
there.** An item carried four versions is an item nobody has decided against —
which is worse than cutting it.

**Still CUT, not deferred:** the contributor leaderboard and side-by-side
character comparison. Neither is in v0.18, v0.19 or anywhere else. Do not
re-inventory them.

---

## What reading the code changed about the list

Four things, all established against the code on 2026-09-08 rather than assumed.
Three of them move an item between categories, which is why they are here and
not in a commit message.

### The reorder mechanism already exists, so two of the three ordering items are small

`js/editor-reorder.js` is v0.15's item 8 and its header states the intent
plainly: *"A ninth strip added later gets reordering by declaring where its array
lives."* Seven strips declare one today (`editor-system.js:18,35`,
`editor-tabs.js:183,218,247,294,337`).

* **F3, Combo Lists** — the control is simply not rendered on that strip. The
  submit side is **already done**: `desc.comboList` is in `EXTRA_KEYED_SECTIONS`
  (`js/character_tabs.js:410`) keyed by `starter`, and `scanEveryOrder`
  (`js/editor-core.js:1246`) walks every keyed section. So this is a render
  change, not a pipeline change.
* **F4, rows inside a Combo List** — genuinely different and **not** free. Rows
  are table rows edited through a modal (`js/editor-tabs.js:1148-1203`), not
  `.daw-tab-item` entries, so the shared strip mechanism does not reach them.
  The owner's *"not sure why this isn't implemented yet, but it should be
  simple"* is right about the outcome and not about the route.

### B2 was filed as a feature and is a data-loss bug

The owner's list asks to *"add ordering to System Type pages navigation tab"*.
**The control is already there** — `editor-system.js:18` renders
`reorderStripControls('desc.tabs')`, and clicking it does move the array.

What is missing is that **nothing records the move**. `buildSystemDeltas`
(`js/editor-system.js:218`) emits `system_tab` per tab and `system_section` per
section, and section order rides along as `meta.order` on the tab payload
(`:239`). No delta ever names the **sequence of tabs**. So reordering tabs
changes no tab's metadata and no section's content, `differs()` finds nothing,
and the contributor is told "no changes detected".

That is the same class as the v0.15 finding that *reordering never submitted and
never applied, because position was not in the delta model at all* — fixed then
for character pages, never for the tab strip on system pages.

It moves to Bugs because the difference matters to how it is verified: a missing
feature is proven by building it, and silent data loss is proven by **losing
something first**. The spec below starts from a repro.

### FT5 stops being optional

B2 introduces a new system-page delta shape. FT5 is "Diff View renders the new
system-pages engine". Shipping B2 without FT5 means reviewers approve a tab
reorder they cannot see — which is bug 4's lesson from v0.15, in the exact same
place. **They land together.**

### B1 is diagnosed, not merely reproduced

`js/editor-reorder.js:171-207`. `afterChange()` re-renders the strip through
either the registered refresh hook or `initFullTabEditor`, both of which draw
the strip with their **default** selection — `editor-tabs.js:299` hardcodes
`class="daw-tab-btn active"` onto the first button. Only after that does
`reselect(spec, index + dir)` find the right button and `.click()` it.

So the frame the owner sees is real and is exactly one render: default selection
painted, corrected on the next tick. *"They very very briefly go to the first
place and then back to their newly changed place."*

The fix is to render **with** the target index selected rather than
render-then-click. Not a transition or an animation suppression — those would
hide the flash while leaving two renders where one belongs.

---

## Batch 1 — B1, B2, FT5, F3

Sequenced first because both bugs are diagnosed, B2 is discarding the owner's
edits today, and F3 is a render change next to work already open in the same
files.

### B1 — carry the selection through the re-render

`window.setReorderRefresh(fn)` currently takes a hook with no argument and the
hook re-selects whatever it defaults to. Give the refresh the index it should
land on, and drop the `reselect()` round trip on the paths that can honour it.

`reselect()` stays for the strips whose refresh cannot take an index — it is the
fallback, not the mechanism. A strip that keeps `reselect` still flashes, so the
verification below enumerates strips rather than checking one.

**How it is proven:** a spec that moves an entry and asserts the strip is
**never** painted with index 0 active between the click and the settled state.
Asserting only the final state passes today — the bug is entirely in the
intermediate frame, and a test that cannot see the intermediate frame is testing
nothing. `MutationObserver` over the strip, recording every `active` index.

### B2 — a tab-order delta for system pages

Shape follows `meta.order`, which already solved this one level down:

> *The order travels as a list of KEYS. A reorder that carried section content
> would let moving a section overwrite an edit somebody else made to it in the
> meantime — the whole-document bug in miniature.* (`editor-system.js:236-238`)

So the tab order travels as a list of **tab keys**, in its own scope, carrying
no tab content. Same reasoning one level up.

Open, to settle before writing SQL or the payload:

1. **Scope name.** `system_order` is the obvious one and sits beside
   `system_tab` / `system_section` without overloading either.
2. **Backward compatibility.** `scanOrder` in `editor-core.js:1197` refuses to
   emit when identities are missing or duplicated, and `applyDeltaToData`
   refuses the same two shapes — *"Losing a reorder is visible and repeatable;
   shuffling somebody's page is neither."* Tab keys must be checked for the same
   two failure modes before this emits anything.
3. **Merge.** `js/admin-merge-compiler.js` batches `system_section` deltas by
   key. Two contributors reordering the same tab strip is a genuine conflict,
   not a merge — decide before building, not at the first ticket.

### FT5 — Diff View renders the system-pages engine

`js/admin-diff.js:514` already walks `currentPendingDescData.tabs`, so this is
not a blank start. What FT5 has to end at is **every** system-page delta scope
having a rendering, including B2's new one, verified by making the picker and
the renderer disagree in both directions:

> *"Everything I offer resolves" and "everything real is offered" are different
> claims.* — v0.15's section-link picker was internally consistent and blind to
> a third of the site.

### F3 — the Combo List strip gets its controls

`reorderStripControls('desc.comboList')` on the strip that renders the list
sub-section, plus `registerInserter` for the same spec so `+` inserts in place
rather than appending.

**The submit path needs no change**, and that claim is the one to falsify first:
if an order delta does not appear when only the order changed, the assumption
that `scanEveryOrder` covers `comboList` is wrong and the item is bigger than
this section says.

The Techs tab shares this machinery through the same vocabulary, so `techList`
gets it in the same change or the two diverge — the exact drift
`js/character_tabs.js`'s own header exists to prevent.

---

## Known-open, carried in — the owner's

None of these is a v0.18 item unless the owner says so.

* **The `isWip` data pass** — every `navigation.json` entry is `true`, so the
  WIP marker means nothing on the roster.
* **v0.15 carries** — the leaked-password toggle, ~90 blank inputs on Star Rage,
  the contributor input guide.
* **The 11 CMS pages with no `page_data` row** — Gamemodes, Misc, Servers,
  M1 Trading among them.
* **Sky Assassin's colour code.** The icon arrived 2026-09-08 (PR #176) and is
  now on both branches; the colour has not.
* **A username (`@handle`)** is wanted and deliberately deferred. Scope it in
  the same version as **@mentions or public profile URLs**, whichever is
  scheduled first, and never as a standalone item — on its own it has no
  consumer. Full reasoning in `devlogs/V0.17-DEVLOG.md`.

---

## The suite is close to its limit

Recorded here because it is not any one version's problem and will keep
surfacing as one.

**1452 tests against a single `python -m http.server` with 4 workers.** At the
v0.17 release a slow CI runner turned one flaky test into eight plus a hard
failure; a re-run of the identical commit came back **1439 passed, 1 flaky, 12.6
min**. Nothing was wrong with the code, and half an hour went into proving it.

The failure mode to recognise: **a burst of flakes across unrelated
page-loading specs, with the run time up by a quarter.** That shape is load, not
a regression. Read the conclusion (`CANCELLED` is not `FAILURE`), compare the
duration against recent runs, and re-run before diagnosing.

The real fix is a threaded server or parallel-safe fixtures. Not urgent; it will
stop being optional when a release is blocked by it rather than delayed.
