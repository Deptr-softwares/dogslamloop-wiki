# v0.18 — devlog

**NOT YET SCOPED.** This file is the SPEC once it is: design decisions get
recorded here *before* they are built, with data shapes and delta scopes worked
out. Read the whole of a section before building from it — v0.15's Combos tab
was built to an invented shape while the real one sat in the same file.

v0.17 shipped 2026-09-03 as PR
[#156](https://github.com/Deptr-softwares/dogslamloop-wiki/pull/156), merge
commit `07eb647`. Its record is `devlogs/V0.17-DEVLOG.md`, and it is worth
reading before starting here — the expert system, user profiles and the person
record all landed there, and the first item below builds directly on the last.

---

## Inventory, carried from v0.17 — **not** a scope

The owner scopes a version in their own words when its turn comes. This is the
list waiting for that conversation, not a decision.

* **Media Library uploader credit** *(carried 13)*
* **Character Gallery tab** *(carried 3)* — a real build, not a finish.
  `js/character_tabs.js` says the tab *"renders empty on the reader page and is
  absent from every admin and editor list."*
* **`CONTRIBUTING.md`** — only a two-line README exists today
* **FT5** — Diff View renders the new system-pages engine
* **FT6** — editing a system page renders only the section being edited
* **FT8** — stress-test the `internalStyling` rules for edge cases

**Uploader credit now has the person record it was waiting for.** v0.17's
argument for building profiles first was that the expert badge, profiles and
uploader credit all wanted *a place where a person exists on this site*. Two of
those three are spent; this is the third. `user_profiles` is keyed by `user_id`,
`get_public_profile(uuid)` and `get_public_profiles(uuid[])` read it, and
`get_user_expert_pages(uuid)` is the shape to copy for "what has this person
done" — so uploader credit should be a join, not a new denormalized name column.

Read `devlogs/V0.17-DEVLOG.md`'s F11 section before designing it. The reason the
display name is **not** a column in `user_profiles` is the part most likely to
be undone by accident.

---

## Known-open, carried in

These are the owner's, and none of them is a v0.18 item until they say so.

* **The `isWip` data pass** — every `navigation.json` entry is `true`, so the WIP
  marker means nothing on the roster.
* **v0.15 carries** — the leaked-password toggle, ~90 blank inputs on Star Rage,
  the contributor input guide.
* **`visual.spec.js` baselines are stale.** All three diffs at the v0.17 release
  were explained (the intended nav changes, live-content drift, and the
  discussion author becoming a button), but the tool is not useful for the next
  CSS pass until they are refreshed. **Its own change, never folded into a
  deploy** — it is `testIgnore`d in CI, so nothing there will catch a mistake in
  it.
* **A username (`@handle`)** is wanted and deliberately deferred. Scope it in the
  same version as **@mentions or public profile URLs**, whichever is scheduled
  first, and never as a standalone item — on its own it has no consumer. The
  full reasoning is in `devlogs/V0.17-DEVLOG.md`.

---

## The suite is close to its limit

Recorded here because it is not any one version's problem and will keep
surfacing as one.

**1452 tests against a single `python -m http.server` with 4 workers.** At the
v0.17 release a slow CI runner turned one flaky test into eight plus a hard
failure; a re-run of the identical commit came back **1439 passed, 1 flaky, 12.6
min**. Nothing was wrong with the code, and half an hour went into proving it.

The failure mode to recognise: **a burst of flakes across unrelated
page-loading specs, with the run time up by a quarter.** That shape is load, not
a regression. Read the conclusion (`CANCELLED` is not `FAILURE`), compare the
duration against recent runs, and re-run before diagnosing.

The real fix is a threaded server or parallel-safe fixtures. Not urgent; it will
stop being optional when a release is blocked by it rather than delayed.
